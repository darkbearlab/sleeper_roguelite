# 新地圖編輯器 — 規劃 + 實作狀態

> 狀態：**v1 編輯核心已實作**（`editor.html`，2026-06-23）。使用者核可：A 獨立檔、B v1 不做試玩、C 先做編輯核心 1–4。
> 實作含 1–4＋必要的匯出/匯入（否則工具無法產出）；**延後**：具名 localStorage 草稿管理、檔案下載/上傳、試玩接縫(index `?test`)。
> 對照：原「遊戲內編輯器」(scene EDITOR，見 開發進度與脈絡.md §4.13) 仍保留並存；本工具為獨立 DOM 版。
>
> **已實作摘要**：`editor.html` 自足單檔，畫布平移(中鍵/空白拖)+縮放(滾輪)+格線+fit；三分頁；undo/redo；
> 內嵌 alpha/bravo/outpost/lanes 範本可載入編輯；① 牆/門/低矮牆/破壞物(5預設)/草叢/按鈕(連動門)/進撤點 拖曳放置+端點拖移+屬性面板；
> ② waypoint 加/拖/刪(**刪點自動重編號 edges+spawns**，因遊戲以陣列索引當 id)、連線(點兩點建/斷、點線上刪邊、rubber-band)、節點檢視(座標/連線數/標籤)、**邊穿牆即時紅標**；
> ③ 敵人配置(類型/個性/朝向把手+視野錐預覽)、俘虜；匯出 純JSON(可再匯入 round-trip) / MAPS 片段(貼回 index.html)。headless 37 條驗證(重編號/round-trip/片段可eval/驗證/三分頁逐幀render)。

## 1. 目標與範圍（來自需求）
- 獨立工具，**可匯出與讀取（再次編輯）**編輯結果。
- **三分頁**：① 地圖物件 ② waypoint 與連線 ③ waypoint 上的敵人配置（**不改遊戲現有生成方式**＝沿用 `enemySpawns`）。
- 地圖物件與 waypoint 皆可**拖曳調整**；**連線編輯要特別下功夫**。
- 匯出的地圖可再匯入編輯。
- 含**試玩**功能（因此放不同檔較合理）。

## 2. 架構決策（建議）
- **新檔 `editor.html`（獨立、自足）**：自帶畫布＋DOM 介面＋編輯邏輯，可單獨開啟。
- **`index.html` 維持「單一檔、開檔即玩」**——遊戲本體不依賴編輯器。只加一個**極小的試玩接縫**（`?test=1` 時從 localStorage 讀地圖、直接進一場戰鬥；正常開檔路徑完全不變）。
  > 「單一檔」原則是針對**可出貨的遊戲本體**；編輯器是開發工具，獨立檔不違反此原則。
- **資料回流遊戲**：編輯器「匯出 JSON」→ 你貼回 `index.html` 的 `MAPS`（保持 index 自足）。另提供「複製為 MAPS 片段(JS)」一鍵生成 `key: {…}` 方便貼上。
- **不動既有 in-index 編輯器**：兩者先並存；新編輯器成熟後再考慮淘汰舊的。

## 3. 資料格式（與遊戲 `MAPS` 完全一致 → 可雙向 round-trip）
編輯器的 in-memory 模型 = 遊戲地圖物件本身，匯出即此 JSON：
```
{
  label: string,
  timeBudget?: number,                       // 省略＝用全域 CONFIG.timeBudget
  walls:        [{x1,y1,x2,y2}],
  doors:        [{id, x1,y1,x2,y2, open?}],  // id 供按鈕連動
  lowWalls:     [{x1,y1,x2,y2}],
  bushes:       [{x,y,w,h}],
  destructibles:[{x1,y1,x2,y2, hp, intact, broken}],   // intact/broken ∈ DESTRUCT 形態名
  buttons:      [{id, x, y, doorId}],        // 遠端開門
  waypoints:    [{id, pos:{x,y}, tags:[]}],
  edges:        [[a,b], …],                  // 無向（遊戲雙向加鄰接）
  entryPoints:      [{x,y}],
  extractionPoints: [{x,y}],
  enemySpawns:  [{nodeId, type?, personality?, facing?}],  // type∈ENEMY_TYPE_IDS;省略type＝grunt
  prisoners:    [{x,y}],
}
```
- **匯入**＝貼上/讀檔/選範本，解析成同結構。匯出/匯入互為逆。
- 編輯器需要的少量目錄（`ENEMY_TYPE_IDS`、waypoint 標籤清單、`DESTRUCT_PRESETS`、`DESTRUCT` 形態名）在 editor.html 內各放一份小常數（與 index 同步維護；數量少、變動低）。

## 4. 共用畫布 + 三分頁模型
- **單一世界畫布**所有分頁共用，支援 **平移(空白鍵拖/中鍵) + 縮放(滾輪)**（預留「地圖可更大」）。座標皆世界座標。
- 切分頁＝切換「**哪一層可互動/可編輯**」與右側屬性面板；其餘層淡色顯示作為對位參考。
- 共用：格線吸附(可調格距/開關)、復原/重做(undo/redo stack)、即時驗證列。

## 5. Tab 1 — 地圖物件
- 工具：選取 / 牆 / 門 / 低矮牆 / 破壞物(+預設下拉) / 草叢 / 按鈕 / 進場點 / 撤離點 / 刪除。
- **繪製**：線段類(牆/門/矮牆/破壞物)拖曳兩端點成線；矩形類(草叢)拖曳成框；點類(按鈕/進/撤)單點放置。
- **拖曳編輯**（選取工具）：拖整個物件移動；拖線段端點 / 矩形角點微調；點物件直接拖。
- **屬性面板**：選中物件的精確座標(可改數字)、破壞物預設/HP、門 `open` 預設、**按鈕→門 連動(doorId 下拉)**。
- 刪除：右鍵點物件 / 刪除工具 / 選中後 Del。

## 6. Tab 2 — Waypoint 與連線（**重點**）
- **waypoint**：放置自動編號(id)、拖曳移動(id 不變)、刪除時自動移除其相關 edges 並（選項）重編號。
- **連線編輯（多下功夫）**：
  - **拉線建邊**：從一個 node 拖到另一個 node 放開＝建立 edge；放開時有 rubber-band 預覽線跟著游標。
  - **點兩下建/刪**：點 node A 再點 node B＝切換該邊有無。
  - **刪邊**：滑鼠移到邊上高亮 → 點擊/右鍵刪除。
  - **節點檢視**：選中 node 顯示 id/座標/度數(邊數)/鄰居清單（點鄰居可跳選或一鍵斷開）。
  - **即時卡牆檢查**：邊若穿越牆 / 擋移動的破壞物 → 該邊畫紅 + 列入警告（沿用現有幾何規則，避免 AI 卡牆）。
  - 便利（選配）：依鄰近自動連線、孤點偵測高亮。
- **標籤**：waypoint 標籤(patrol/cover/flank/vantage/rush/choke/spawn) 以屬性面板勾選集設定（亦可標籤刷）。

## 7. Tab 3 — 單位配置（敵人 / 俘虜）
- **不改生成方式**：敵人＝掛在 waypoint 上的 `enemySpawns`。
- **指派**：點 waypoint → 設/循環 敵人類型(grunt/dog/heavy_lmg/heavy_shot/無)；設個性覆寫(rusher/flanker/holder…)、**朝向 facing**（拖一個方向把手或填角度）。
- **預覽**：在該 waypoint 畫敵人圖示＋朝向箭頭＋**視野錐預覽**（直接驗證哨兵守備角度/側翼缺口，呼應潛行/側翼設計）。
- **俘虜**：本頁放置/拖曳 `prisoners` 點（救援目標）。
- 屬性面板：每個 spawn 的 type/personality/facing 下拉與角度。

## 8. 驗證（即時、阻擋試玩/匯出時警告）
沿用並擴充現有 `editorValidate`：① ≥1 進場點、≥1 撤離點 ② 有 waypoint 且無孤立節點 ③ **edge 不穿牆/擋移動破壞物** ④ `enemySpawns.nodeId`/`buttons.doorId` 指向存在的目標 ⑤ 進/撤點不在牆內。畫面常駐檢查列 + 紅色標記。

## 9. 存讀 / 匯出匯入 / 試玩
- **本機草稿**：多個具名地圖存 `localStorage['sleeper_editor_maps'] = {name: mapJSON}`；新建/儲存/另存/載入/刪除/複製。
- **檔案**：下載 `.json`(Blob) / 上傳 `.json`(file input) — 讓地圖可離開瀏覽器、利於真正製作關卡。
- **匯出 MAPS 片段**：一鍵產生可貼回 index.html 的 `key: {…}` JS。
- **試玩**：序列化現圖 → 寫 `localStorage['sleeper_editor_testmap']` → 開 `index.html?test=1`(新分頁)。
  - **index.html 試玩接縫（唯一改動）**：載入時若 `?test=1` 且有該 localStorage → 註冊成臨時 `MAPS.__test`、`buildMapDerived('__test')`、用預設 roster 直接進一場 COMBAT（跳過序章/路線圖）；提供離開回到提示。正常開檔不受影響。

## 10. 與現有 in-index 編輯器的關係
- 先**並存**：舊編輯器(scene EDITOR)保留，新編輯器獨立。新版穩定後可把遊戲內入口導向新版或移除舊版（屆時再議）。

## 11. 實作里程碑（核可後）
1. `editor.html` 骨架：自足畫布 + pan/zoom/grid + 三分頁切換 + 共用模型/undo。
2. Tab 1 地圖物件（含拖曳端點 + 屬性面板）。
3. Tab 2 waypoint + 連線（拉線/點選/刪邊/節點檢視/卡牆檢查）。
4. Tab 3 敵人/俘虜配置（朝向把手 + 視野錐預覽）。
5. 驗證列 + 本機草稿 + 檔案匯出匯入 + 「複製為 MAPS 片段」。
6. index.html 試玩接縫 + 編輯器「試玩」鈕。
7. 驗證：editor.html 也用 headless harness 跑（DOM stub）；確認匯出 JSON 能被 index 的 `buildMapDerived` 正常吃下、幾何不卡牆。

## 12. 待你拍板（核可前）
- **A. 架構**：採「獨立 editor.html + index 加極小試玩接縫 + 匯出 JSON 貼回 MAPS」？（建議）
- **B. 試玩**：用「localStorage 交接 + `?test=1`」即可？或你想要其他方式（例如不改 index、改成編輯器內嵌簡化預覽）？
- **C. v1 範圍**：上述 1–6 一次做齊，或先做 1–4（編輯核心）再補 5–6（存讀/試玩）？

> **核可結果（2026-06-23）**：A 獨立檔；B v1 不做試玩；C 先做編輯核心 1–4。已實作（見本檔頂端「實作狀態」）。

---

# v2 規劃 — 地圖特徵(prefab) + 群組 + 重疊放置（提案，待核可）

> 需求（2026-06-23）：① Tab1 增加「可直接放入一整個有意義地圖特徵」的東西（柱子/簡易房間/長廊/轉角…），且**特徵能整個一起放大縮小與位移**。② 物件可放到已有東西的位置上（例如門疊到牆上，含特徵）。

## v2-1 為什麼需要「群組」（核心難點）
遊戲的地圖格式是**扁平的基本物件陣列**（walls/lowWalls/doors/…），沒有「群組」概念。一個「房間」其實是好幾條牆。若直接展開合併進陣列，特徵就**失去整體身分**，事後無法整組選取縮放。要滿足「整個一起放大縮小位移」，需要一層**編輯器專用的群組標記**。

**做法（建議）**：在每個基本物件上加一個**編輯器專用欄位 `_gid`（群組 id）**。同 `_gid` 的物件＝一組。
- 群組是「真實基本物件的集合＋一個外框變換」，**不是另一種物件**；個別成員仍可單獨微調。
- **匯出**：`_`開頭欄位皆為編輯器專用。「純 JSON」(編輯器存檔) **保留 `_gid`**（群組可 round-trip）；「MAPS 片段」(貼回遊戲) **剝除所有 `_*`**（遊戲只讀已知陣列，乾淨）。遊戲本來就忽略未知欄位，故即使殘留也無害。

## v2-2 特徵庫（FEATURES，參數化產生器）
每個特徵：`{ id, name, build(box, opts) -> [基本物件…] }`，由你**拖一個外框**決定大小（格線吸附），放下時：產生對應基本物件 → 全部蓋同一個新 `_gid` → 自動選取整組並顯示變換外框，可立刻續調。
- v1 點名四種：
  - **柱子 Pillar**：外框四邊牆（小方塊）。
  - **簡易房間 Room**：外框四邊牆，指定一側留**缺口**（gap，寬度可調）。
  - **長廊 Corridor**：兩條平行牆（水平＝上下、垂直＝左右），兩端開放。
  - **轉角 Corner**：L 形（指定角落，沿外框兩邊各一段牆）。
- 可選加碼：**房間+真門**（缺口處放可開關 door 物件）、**窗牆**（牆＋一段可破壞玻璃 destructible）、**掩體叢**（幾塊低矮牆/破壞物掩體）、**十字路口**。
- v1 特徵只產**幾何**（牆/低矮牆/門/破壞物/草叢），不自動產 waypoint/敵人（導航仍在 Tab2/3 處理），保持單純。

## v2-3 群組操作 + 通用多選
- **選整組**：選取工具點到任一成員＝選**整組**（顯示外框＋8 個控制點）；**Alt+點**＝選**單一成員**微調。
- **位移**：拖外框內部＝整組平移（所有成員座標 +Δ）。
- **縮放**：拖外框控制點＝整組依「對側錨點」縮放（角點＝雙軸、邊點＝單軸；Shift＝等比）。對各型套用：線段兩端點、點物件座標、草叢 x/y 與 w/h、破壞物端點，皆乘上縮放比（HP 等非幾何欄位不動）。最小尺寸夾住避免歸零。
- **旋轉**：**v1 先不做**（草叢是軸向矩形無法旋轉；且你只要求縮放/位移）。要做時僅限不含草叢的群組，再議。
- **通用群組**：框選（在空白處拉方框，選取所有相交物件）→「群組/解散群組」，讓你把任意物件併成一組（特徵只是預先群組好的一批物件）。

## v2-4 重疊放置
- **現況已允許重疊**：各繪製工具一律在游標處新建，不擋既有物件；特徵亦同（放在已有牆上 OK）。本項主要補**重疊時的選取**。
- **重疊選取**：同一點重複點擊＝**循環**選取該處堆疊的候選物件（編輯器常見手法），讓你選到被蓋住的那個。
- **門疊到牆**的語意提醒：遊戲裡實心牆會擋，門需落在**牆的缺口**才有通行意義。兩種處理：
  - (預設) **純允許重疊**，缺口由你自理（用「簡易房間」的缺口、或自行斷牆）。
  - (選配) **疊門時自動在牆上切出對應缺口**：放門若壓在某牆段上，將該牆從門兩端切斷留缺口（較聰明但較多邊界處理）。

## v2-5 實作里程碑（v2 核可後）
1. `_gid` 群組基礎：bbox 計算、整組選取/位移、Alt 選單一、匯出剝 `_*`（純 JSON 保留）。
2. 群組縮放外框（8 控制點，對側錨點，邊/角，Shift 等比）。
3. 框選 + 群組/解散。
4. FEATURES 庫（柱子/房間/長廊/轉角）＋拖框放置→自動成組；工具列「特徵」分區。
5. 重疊選取循環；（選配）門切牆缺口。
6. headless 驗證：群組變換數學、`_gid` 匯出剝除/純 JSON 保留、特徵 build 幾何、round-trip。

## v2-6 待你拍板（核可前）
- **G. 群組模型**：採「持久 `_gid` 群組（可隨時整組縮放/位移，純 JSON 保留、MAPS 片段剝除）」？（建議）或「只在放置當下可調整、放下即散為個別物件」（較簡單，但事後無法整組縮放）。
- **H. 門疊牆**：純允許重疊、缺口自理？（建議）或放門自動切牆缺口？
- **I. v1 特徵清單**：柱子/簡易房間/長廊/轉角必做；是否一併加房間+真門 / 窗牆 / 掩體叢 / 十字路口？

> **核可結果（2026-06-23）**：
> - **G**：**不用持久 `_gid`**。改「放置當下可調 ＋ 事後用**框選多物件**形成**暫時群組**一起縮放/位移」。資料格式維持與遊戲一致（無 `_*` 欄位）。框選即群組——選取集本身就是變換對象。
> - **H**：**放門自動切牆缺口**（`cutWallsForDoor`：門與某牆共線重疊→把該牆從門兩端切斷留缺口）。門工具放置 / 房間+真門 / 門 inspector「切牆缺口」鈕皆觸發。
> - **I**：**全做** 8 種＝柱子/簡易房間/長廊/轉角 ＋ 房間+真門/窗牆/掩體叢/十字路口。
> - 預設：旋轉 v1 不做；縮放支援邊(單軸)/角(雙軸)控制點＋Shift 等比；重疊用點擊循環選取。
> 已實作（editor.html v2）。
