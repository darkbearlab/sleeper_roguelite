# SLEEPER — 開發進度與脈絡（handoff / compact 用）

> 本文件供對話 compact（壓縮）後接手用，盡量自包含。原始設計規格見 `SLEEPER_開發計畫.md`，實作全部在單一檔 `index.html`（原生 JS + Canvas 2D，零外部相依，瀏覽器開檔即玩）。所有可調數值集中在檔頂 `CONFIG` / `WEAPONS` / `ARCHETYPES` / `ABILITIES` / `ROADMAP`。

---

## 0. 一句話定位

俯視戰術射擊 × 附身式小隊操作 × Slay-the-Spire roguelite。一次只完整附身一名單位（FPS 式滑鼠鎖定瞄準、WASD 移動、Shift 奔跑），其餘隊員跑各自固定的「待機個性」本能；每場戰鬥在時間關門、追兵湧入前至少把一人帶到撤離點。撤出者帶入後續關卡、陣亡永久損失。在路線圖上選路前進（戰鬥/休息兩種節點），走到終點＝過關。

---

## 1. 現況總覽

- **Phase 1 垂直切片**：全部硬性需求完成且通過無頭驗證。
- **Phase 2 meta**：已完成「路線圖 + 休息關 + 跨關陣容 + 武器/技能資料模型 + 待機個性矩陣 + 隨機招募」。
- **庫存層**：共用倉庫(有上限、綁生還人數) + 臨時背包 + 戰鬥掉落/拾取 + 搜刮改三選一(地板保證) + 休息關配裝/換技能 + 共用彈藥儲備與手動分配 + 預設武器底線 pistol。詳見 §4.9/4.10。彈藥本階段寬裕不咬人（機制與 UI 已做齊）。
- **地形/互動/敵人狀態機**：兩張戰鬥地圖（`MAPS.alpha`/`bravo`，registry + `buildMapDerived` 切換）；門(可 E 開關、按鈕遠端連動)、低矮牆(掩體)；敵人純狀態機 IDLE↔ENGAGED(latch)。詳見 §4.11/4.12。bravo＝門/掩體圖，敵人預設閒置藏在門後（開門才現形）。
- **關卡編輯器**：遊戲內視覺編輯器（scene EDITOR）。滑鼠放置牆/門/矮牆/waypoint/edge/敵spawn/按鈕/進撤點/**俘虜**、格線吸附、即時驗證、一鍵試玩、輸出 JS 貼回 `MAPS`。詳見 §4.13。
- **Phase 3（最新）**：① 裝備(持續被動)+道具(一次性 Q 鍵) 各 1 槽（`EQUIPMENT`/`ITEMS`，進倉庫/搜刮/軍械庫；裝備效果套 makePlayer、道具 useItem）② **軍械庫節點**(ARMORY：三選一拿武器/裝備/技能/道具→倉庫) ③ **俘虜**(任務內目標物：E 釋放→入我方，撤離成功才入隊) ④ **各關時間** node>map>全域。詳見 §4.14/4.15。槽位用「槽數變數」可未來擴充。
- 多張戰鬥地圖（roadmap `map` 欄指定）：**alpha=開闊庭院、lanes=雙線哨廊、outpost=前哨站v2、bravo=門禁設施(破門原型)**。2026-06-22 重設計：舊圖都是「整牆+單缺口」逼擠同洞、側翼/潛行無從施展 → 改開放+掩體島/平行低矮牆/中央短牆繞道+草叢，**能繞到哨兵側後、能叢間潛行**（舊片段備份 `舊地圖備份.md`）。路線圖以顏色/圖示/標籤+圖例區分。
- **敵人類型（最新）**：`ENEMY_TYPES` 目錄（資料驅動，`enemySpawns.type` 指定，預設 grunt）。除原 grunt(步兵) 外新增 **dog(警犬：快/脆/廣視錐/貼臉連咬)**、**heavy_lmg(重機槍兵：慢/帶甲/點放掃射)**、**heavy_shot(霰彈兵：中速/帶甲/推進貼臉)**。重裝靠「裝甲機率整發擋下非穿甲」形成質差（血量與 grunt 相同）；穿甲(`pierce`)分級：rifle 0.5、dmr 0.9，其餘 0。詳見 §4.16。已放進 alpha/bravo 與編輯器。
- **撤離 QOL**：撤離鍵改 **H**（防誤觸）、**Shift+H 全體撤離**；新增**撤離護送**——附身/切到撤離中單位不再中斷，會持續自走撤離且滑鼠可轉視野，任一指令(移動/開火/技能等)才奪回控制。詳見 §4.7。
- **側翼/突襲（最新）**：依攻擊來向 vs 目標視野錐——**繞背(錐外)＝傷害×2 且無視所有裝甲**（敵擋格＋玩家 dmgResist，甲只罩正面）、**閒置敵正面＝×1.5**，取高不疊，**敵我皆適用**。對重裝＝穿甲武器或繞背兩解；逼用第二單位拉注意力繞後。詳見 §4.17。
- **第0關 / cutscene 導演系統（最新）**：`initRun` 每局先進序章（`intro` 地圖，scene COMBAT + `cutscene` 導演）。流程＝管制室對白→主角往返→**左鍵處決同僚(教開火)**→接手→**E 取得密碼**→**E 開門**→守衛現身密語「For our Father」→**兩守衛入隊(帶槍)**→**H/Shift+H 撤離**→序章休息(教學面板)→路線圖。陣容＝主角(pistol)+2守衛＝正式 run 的 roster。**Esc 只跳演出**（休息仍進、不顯教學）。導演系統(節拍 say/move/face/wait/cam/sub/do/gate)可重用於未來劇情。詳見 §4.18。
- **潛行/警覺層（最新，Stage 1-5 全完成）**：為「攻克通過哨點」。① IDLE 敵**短錐+有限視距**+**識別空窗**(`detect` 累滿才交戰、快閃安全、貼臉瞬識、槍聲/被擊中仍瞬交戰、ENGAGED 永鎖)+overwatch 對 IDLE 收火+識別回饋弧 ② **草叢**(MOBA 遮蔽，叢內對叢外隱形；敵藏/我藏) ③ **消音手槍**(silpistol，不發槍聲，亞音速彈稀少限頻) ④ **望遠**(右鍵：鏡頭前帶+穩定+禁奔跑+示意線拉長；技能移空白鍵) ⑤ **前哨站 outpost**(分層 choke 哨點+草叢繞路)。詳見 §4.19。
- **可破壞地形**：`MAP.destructibles`——一塊地形有 HP + intact/broken 兩形態，被擋下的子彈扣 HP→換形態。五型：木箱(矮牆→平地)、落地窗(玻璃→平地)、有矮牆窗(玻璃→矮牆)、可破壞全遮蔽(牆→矮牆)、鐵絲網(看穿/穿彈/打不壞)。玻璃非穿甲擋穿甲穿；敵我子彈都能破。編輯器有 destruct 工具+預設；alpha 放四型示範。詳見 §4.20。
- **數值平衡面板**：按 `` ` `` 或右上齒輪開的 DOM 側欄，直接寫入 CONFIG/WEAPONS/ENEMY_TYPES/ABILITIES/EQUIPMENT/ITEMS 的活值＝多數即時生效；依物件結構自動生成滑桿/勾選(新增數值不必改面板)、四分頁+搜尋、localStorage 持久化、輸出覆寫/全部重設。資料層與 DOM 分離(headless 可測)。詳見 §4.21。
- **獨立地圖編輯器 `editor.html`（最新）**：與 index 分檔的自足 DOM 工具，三分頁(地圖物件/waypoint+連線/敵人配置)、拖曳編輯、連線編輯加強、邊穿牆紅標、與遊戲 MAPS 同結構可雙向 JSON round-trip＋「MAPS 片段」貼回。內嵌現有地圖為範本。**踩雷：刪 waypoint 必須重編號(遊戲以陣列索引當 id)**。詳見 §4.22 與 `地圖編輯器規劃.md`。**概念驗證告一段落，現轉入真正關卡製作＋平衡。**
- 美術全為幾何佔位；數值全為佔位待調。

---

## 2. 場景狀態機（`scene`）

```
MAP(路線圖) ──選 COMBAT 節點──► COMBAT ──勝利(撤出≥1)──► 回 MAP（開放後繼節點）
     │                              └──全滅(撤出0)──► RUNEND
     └──選 REST 節點──► REST(二選一) ──► 回 MAP
走到無後繼的終點節點 ──► RUNWIN
```

- `scene ∈ { MAP, COMBAT, REST, ARMORY, RUNEND, RUNWIN, EDITOR }`。EDITOR＝關卡編輯器（路線圖右上鈕進入）；ARMORY＝軍械庫節點三選一。**第0關(序章)沿用 COMBAT scene + `cutscene` 導演**（不另開 scene）；見 §4.18。
- 流程函式：`initRun()`（開新 run）→ **`startIntro()`（第0關，可 Esc 跳）→ 序章休息 → MAP** → `selectNode(id)`（進節點）→ 戰鬥 `endMission()` / 休息 `restDoContinue()` → `advanceNode()`（標記完成、開放後繼或 RUNWIN）。
- **戰鬥在滑鼠鎖定後才開始推進**：`update()` 開頭 `if (scene!=='COMBAT') return; if (!pointerLocked) return;`（未鎖定＝暫停；Esc 解鎖＝暫停）。**鎖定前 `draw()` 走 `drawCombatStart()`＝全黑螢幕+「點擊以開始」**（不繪世界、不洩漏場面；全關卡適用，含第0關）。

---

## 3. `index.html` 內部分區地圖（依出現順序）

| 區段 | 關鍵符號 | 職責 |
|---|---|---|
| CONFIG | `CONFIG` | 所有可調數值 |
| 武器/技能/個性目錄 | `WEAPONS`,`WEAPON_IDS`,`ABILITIES`,`ARCHETYPES`,`ARCHETYPE_IDS`,`TIER_COLOR`,`UNIT_NAMES`,`HUE_POOL`,`rngPick`,`makeRecruit` | 資料驅動內容 + 隨機招募 |
| Math | `dist,angTo,angDiff,rotToward,clamp,segIntersect,closestOnSeg,segCircle,raySegT,rayHit`(含關門) | 向量/角度/幾何/射線 |
| Map | `MAPS`(alpha/bravo),`MAP`(作用中,let),`WORLD`,`rectWalls`,`buildMapDerived`,`adj`,`wp` | 多張戰鬥地圖 registry + 衍生表重建 |
| ROADMAP | `ROADMAP`(COMBAT 節點含 `map` 欄),`roadAdj`,`roadNode`,`roadMaxCol`,`roadNodePos` | 路線圖 DAG |
| LoS/Vision | `segBlocked`,`clearLine`(牆+關門),`clearLineWalls`(僅永久牆,給 nodeVis),`nodeVis`,`nearestNode`,`extractNodeId`,`nextHop`,`buildNextHop`,`canSee`,`isRevealed` | 視線(門動態)、節點可見度表、撤離 next-hop、感知 |
| Camera | `cam`,`camTarget`,`initCamera`,`updateCamera`,`w2s`,`applyCam` | 旋轉跟隨鏡頭 + 世界→螢幕轉換 |
| 武器/技能/裝備/道具目錄 | `WEAPONS,ABILITIES,EQUIPMENT,ITEMS` + `*_IDS`/`LOOT_*_IDS` | 4 類資料驅動內容 |
| State | `players,enemies,bullets,particles,drops,prisoners,controlledId,reservations,scene,curTimeBudget,forcedMap,fromEditor,edit/editTool/...,roster,missionIndex,warehouse,backpack,ammoStock,armoryOffers,restPhase/Offers/...,roadCompleted/Available/Current,lastSummary,...` | 全域狀態 |
| 單位建立 | `makePlayer`(套裝備效果),`makeEnemy`,`freePrisoner` | 從 roster/spawn 建單位 |
| Run/Mission/Rest | `baseRoster,rosterFromPlayer,spawnPos,releasePointer,initRun,selectNode,advanceNode,startMission`(forcedMap/curTimeBudget/重置門+俘虜)`,endMission`(fromEditor 返回) | meta 流程 |
| 休息關 meta 層 | `enterRest,rollScavenge,...,applyOffer`(含 equip/item)`,restDoAwaken,...,equipWeaponFromWarehouse,unequipWeapon,equip/unequipAbility,equipGearFromWarehouse,unequipGear`(通用裝備/道具)`,discardWarehouse,allocAmmo` | 倉庫/搜刮/配裝/彈藥 |
| 軍械庫 | `enterArmory,rollArmory,armoryPick,drawArmory` | ARMORY 節點三選一 |
| 掉落/拾取 | `spawnDrop,updateDrops,drawDrops,spawnPickup,drawPrisoners` | 戰鬥內物品/俘虜 |
| 互動 | `doorMid,distToDoor,nearestInteractable`(門/按鈕/俘虜)`,tryInteract,freePrisoner` | E 開關門/按鈕/釋放俘虜 |
| 關卡編輯器 | `editorOpen/Exit/Load,blankEditMap,normalizeEdit,snapPt,editorMouseDown/Up,editorClickAt`(含 prisoner)`,editorCommitSeg,editorEraseAt,removeWaypoint,toggleEdge,cycleSpawn,editorValidate,editorTestPlay,editorExport`(含 prisoners/timeBudget)`,drawEditor/EditGeom/EditorUI,eBtn` | scene EDITOR：視覺放置/驗證/試玩/輸出 |
| Possession | `activePlayers,possess,toggleExtract,autoPossess` | 附身/撤離/自動接手 |
| Combat | `fireUnit`(發射原點+spreadMul/buffFire+`alertEnemiesNear`),`unitMag,startReload,tickAmmo`(reloadMul/buff倒數)`,useAbility,useItem`(道具),`manualFire,enemyFire,idleEngageFire,damageUnit`(減傷+命中→ENGAGED),`killUnit,updateBullets`(門/低矮牆) | 開火/換彈/技能/道具/傷害/子彈 |
| IdleAI | `idleTick(u,dt,lockFacing)` | 我方待機個性（視線+紀律+開火） |
| EnemyAI | `isSpecial,releaseReservation,commitReservation,nearestFriendly,scoreNode,watchPoint,enemyTryOpenDoors,enemyIdleTick,enemyTick`(IDLE/ENGAGED 純狀態機) | waypoint 選點 AI + 狀態機 |
| Extraction | `extractTick`(低矮牆減速) | 撤離自走 |
| Movement | `sprintMult,moveToward,pushOutSeg,collideWalls`(門/低矮牆),`onLowWall,lowWallSpeedMult,separate` | 移動/碰撞/掩體 |
| Particles | `spawnHit/Death/Heal,mkPart,updateParticles` | 特效 |
| Input | 滑鼠 pointerlock / `keydown`（依 scene 分支） | 操作 |
| Update | `update(dt)` | 固定時間步主更新（僅 COMBAT+鎖定時跑） |
| Render | `draw` + 各 `drawXxx` | 依 scene 分支渲染 |
| Main loop | `frame` | rAF + 累積器固定 dt |

---

## 4. 各系統詳解

### 4.1 附身 / 控制
- `controlledId`（players 索引或 null）。`possess(i)`：舊控制者→IDLE 並**記住 idleAnchor=facing**；新單位→CONTROLLED、**facing 與 aimDir 恢復到該單位 idleAnchor**（接手時朝它原本盯的方向，不是它飄到的角度）、清 followExtractId。切換無 CD（`switchHandoverMs=0`）。
- `toggleExtract()`（F）：被附身者→EXTRACTING、設 idleAnchor=facing、`controlledId=null`、`followExtractId=該單位`（鏡頭續跟）。
- `autoPossess(fromPos)`：**非撤離(IDLE)優先，無則退而附身撤離中(EXTRACTING)單位**（抓回手動）。
- **無空轉保險**（`update` step 7.5）：戰鬥中 `controlledId==null && followExtractId==null && 有 active 單位` → `autoPossess(null)`。刻意跟隨觀看期間（followExtractId 有值）不觸發。

### 4.2 旋轉鏡頭 + FPS 瞄準
- 鏡頭跟隨置中被附身單位、旋轉使其**朝螢幕上方**；`camTarget.angle = -π/2 - facing`，`cam` 以 followLerp/rotateLerp 平滑插值。
- **anchorY=0.66**：單位畫在畫面 66% 高（偏下）→ 前方視野更長。`applyCam`/`w2s` 用 `cv.height*anchorY` 當縱向錨。
- **瞄準＝Pointer Lock 滑鼠鎖定（mouselook）**：點擊鎖定，`mousemove.movementX × mouseSensitivity` 累積 `aimDir`，砲塔以**武器 turnSpeed** 追 aimDir（追不上＝轉向延遲；停滑鼠就停，無自轉）。
- ⚠️ 取捨：「恆朝上」與「即時絕對瞄準」數學上不可兼得，故為相對轉向，turnSpeed 是手感旋鈕。
- MAP/REST/結算用正常游標（不鎖定），`mouse.mx/my` 由 `canvasPos` 追蹤供點選。

### 4.3 戰爭迷霧（敵我共用）
- `canSee(observer, targetPos)` = 有視線(`clearLine`) ∧（在 `senseRadius` 內 360° ∨ 在前方 `coneHalfDeg`(45°) 視錐內）。**視錐距離無限、靠牆截斷**；senseRadius × 單位 strength。
- 玩家畫面：**離屏 canvas 遮罩**（`fogCanvas`/`fctx`）：填霧 → `destination-out` 挖視錐扇形(`visionPolygon` 射線扇)＋近身圈 → `ctx.drawImage` 貼回主畫布。可見孔露世界、其餘壓黑；霧上再疊淡牆骨架(`drawSkeleton`)當空間參考。`G` 除錯時關霧。
- AI 也吃同一套 `canSee`：敵人/待機只對看得到的目標開火、繞錐外＝看不到。
- ⚠️ 踩雷紀錄：**不可在主畫布直接 destination-out**（會擦掉世界露出底色，敵人/子彈消失但血條還在）——必須走離屏遮罩。

### 4.4 武器 / 開火（發散 bloom）/ 換彈 / 彈藥
- `fireUnit(u, angle, spreadDeg, fireRateMult, bloomAdd) → bool`（回傳是否真開槍，供 TAP 計發數）。
- **手動/敵人** `manualFire/enemyFire`：spread = `spreadBase + u.bloom`，bloomAdd = `spreadPerShot`（連射越散、停火 `spreadRecover` 回復）。玩家點放準、噴射散＝技巧。
- **待機** `idleEngageFire`：spread = `spreadBase/idleAccuracyMult + u.bloom`（基礎較差），射速 ×`idleFireRateMult`，會累積 bloom。
- 換彈：`magazine/reloadTime/reserveAmmo`，空彈匣自動換、`R` 手動換；`tickAmmo` 處理換彈完成補彈 + bloom 衰減。**備彈暫訂 9999**（基本不咬人）。
- **Shift 奔跑** `sprintMult(u) = clamp(base − (weapon.weight−1)×weightPenalty, minMult, base)`，只對被附身且移動中的單位；現值 SMG×1.90 / PDW×1.80 / Rifle×1.50。

### 4.5 待機個性矩陣（ARCHETYPES）
- 每名單位招募時隨機抽一個 archetype；`makePlayer` 由 archetype 衍生 `idleStrategy{fireMode,targeting:'NEAREST_IN_LOS',facing,discipline}`。
- **視線軸 facing**：`HOLD`(維持 idleAnchor) / `SWEEP`(以 idleAnchor 為中心 ±sweepAmpDeg 掃視) / `WANDER`(只在 idleAnchor ±`wanderAmpDeg`(28°) 內、以 `wanderSpeed`(45°/s) 緩慢小幅擺動) / `TRACK`(轉向看得到的最近威脅)。
- **idleAnchor**＝單位「進入閒置那刻的正前方」，在 CONTROLLED→IDLE（possess）與 toggleExtract 時設定；SWEEP/WANDER 以它為中心、HOLD 維持它、附身時恢復到它。
- **射擊紀律 discipline**：`AUTO`(扣到底：持續噴、bloom 拉滿、耗彈快、壓制) / `TAP`(點放：`idleTap{size:3,gap:0.55}` 短 burst + 停頓讓 bloom 回復、準/省彈/斷續)。在 `idleTick` 內實作 burst 計時(`burstLeft/burstCd`)。
- 8 種（tier 為盯哨可靠度，僅用於卡片名稱顏色 `TIER_COLOR`）：Rock(HOLD/TAP/AGG ★★★)、Sentinel(SWEEP/TAP/AGG ★★)、Suppressor(HOLD/AUTO/SUPP ★★)、Hunter(TRACK/TAP/AGG ★★)、Sprayer(SWEEP/AUTO/AGG ★)、Watchdog(TRACK/AUTO/DEF ★)、Drifter(WANDER/TAP/DEF ☆)、Twitch(WANDER/AUTO/AGG ☆)。
- 設計目的：把「盯哨可靠度」變成抽人的風險（飄忽的人逼你勤切勤盯；穩的人可放著）。與迷霧協同（WANDER/SWEEP 視錐會轉離威脅＝真的看不到）。
- `idleTick(u,dt,lockFacing)`：`lockFacing=true` 時保留開火但不改朝向（撤離用）。

### 4.6 敵人 AI（waypoint 圖，不尋路）
- `enemyTick`：每 frame 在 waypoint 圖選點。候選＝當前節點+鄰居，排除被別人 reserve 的特殊節點。
- `scoreNode` = tag 權重 + 朝最近我方推進項(×`chargeMul` 未交火 / ×`holdProxMul` 已交火) + 側翼角項(Flanker) + 暴露懲罰(被我方 `canSee` 的節點) + 視線項(查 `nodeVis`)。
- **hysteresis**（`hysteresisBonus`，目標節點加分防抖；`H` 鍵切換對照）。
- **reservation**（特殊節點 flank/vantage/choke：選擇當下就佔、改目標/死亡即釋放，不永久鎖死）。
- 開火/朝向受感知限制（`canSee`）；**堅守時視錐朝 `watchPoint`＝最近且朝我方那側的 waypoint**。
- 性格：Rusher / Flanker（`CONFIG.ai`）。

### 4.7 撤離與軟計時（含「護送」模式）
- `extractTick`：沿 waypoint 圖 next-hop 自走到撤離點（`nextHop` 由撤離節點 BFS 預算）+ `idleTick(u,dt,true)` 保留 overwatch 開火（不閃避不繞路）。朝向：**護送中(該單位＝controlledId) 跟 `aimDir`（滑鼠轉視野）**，否則面向移動方向。
- **撤離護送模型**（取代舊 `followExtractId`，已退役）：「護送」＝`controlledId` 指向一個 `EXTRACTING` 單位。此時單位**持續自走撤離**、**滑鼠可自由轉視野**（overwatch 沿你看的方向開火），鏡頭跟它（`updateCamera` 對 CONTROLLED/EXTRACTING 皆跟）。**任一指令輸入 → `seizeIfEscorting` 奪回手動(轉 CONTROLLED、撤離中斷)**：移動(WASD)/開火(左鍵) 在 `update` 開頭偵測；技能(空白/右鍵)/換彈R/道具Q/互動E 在事件處理先 seize。開火＝同幀奪回並射擊。
- 進入護送的路徑：① `H`＝`toggleExtract`（當前 CONTROLLED→EXTRACTING 並留在 controlledId＝護送；再按 H 取消）② 數字鍵切到一個 EXTRACTING 單位＝`possess` 進護送（**不再中斷撤離**）③ `autoPossess` 撿到 EXTRACTING 單位亦然。切離護送單位 → 它續自走撤離。
- **`Shift+H`＝`extractAll`**：所有 CONTROLLED/IDLE 單位一次轉 EXTRACTING，挑一名（原控制者，否則最靠近撤離點者）護送。
- 軟計時：`t > curTimeBudget` 後從進入點持續 spawn Rusher 追兵（間隔遞減），直到場上無我方。
- down=dead 永久移除；場上無 active 單位即 `endMission()`。

### 4.8 路線圖
- `ROADMAP`：手刻 DAG（10 節點、5 直行、COMBAT/REST 兩型；起點 2 個、終點 1 個 COMBAT）。`selectNode`（滑鼠點 `roadNodeAt` 或數字鍵）→ 進節點；`advanceNode` 標記完成、開放後繼，無後繼＝RUNWIN。
- 難度：每清一關（`missionIndex`）多放 1 名 rusher（暫定）。

### 4.9 掉落/拾取（戰鬥內）
- `killUnit` → `spawnDrop`：我方/敵方陣亡單位的武器落在原地（底線武器不掉）；drop 帶 `ammo`（折算量：敵 `salvageEnemy`、我 `salvageAlly`）。
- `updateDrops`（每幀、step6）：**只有被附身單位**走到 `pickupRadius` 內自動撿進 `backpack`（任務中不可動用＝撿/不撿的時間/暴露取捨，無管理）。`drawDrops` 在世界層（受迷霧）。HUD 顯示 BACKPACK/地上件數。
- 掉落只存活於該任務；撿進背包者跨任務累積，直到下個 REST 統一整理。

### 4.10 休息關 meta 流程（共用倉庫 + 二選一 + 配裝 + 彈藥分配）
- 進入：`enterRest` 整理背包（彈藥進儲備、item 進倉庫 haul，可能超上限）。`restPhase`：`CHOICE`(可整理) → `OFFERS`(搜刮三選一)/`TARGET`(指定升級對象) → `DONE`(可整理→繼續)。
- **搜刮**（`restDoScavenge`→`rollScavenge`）：`scavengeChoices`(3) 抽選，**地板保證 offers[0]＝supply**（基本彈藥補給，絕不空手）；天花板可出 weapon/ability(→倉庫)、maxhp(全隊)、personality/discipline(需 `TARGET` 指定成員)。`pickOffer`→`applyOffer`（needTarget 者轉 TARGET→`applyTarget`）。先抽取得→再整理倉庫（acquire-then-sort）。
- **喚醒**（`restDoAwaken`）：**`overCap()` 時禁用**（§5 時序：先把倉庫整理到 ≤ 當前上限才能喚醒，防止用 +slot 回溯救回該丟的東西）。+1 隊員（cap +slotsPerMember）→ `autoEquipFromWarehouse`（取估值更高的倉庫槍，否則保留 makeRecruit 的手邊槍；皆無則底線）。
- **繼續**（`restDoContinue`）：`DONE` 且 `!overCap()` 才放行 → `advanceNode`。
- 配裝（CHOICE/DONE 皆可）：`equipWeaponFromWarehouse`/`unequipWeapon`(→底線 pistol)/`equipAbilityFromWarehouse`/`unequipAbility`/`discardWarehouse`/`allocAmmo`。底線武器換下即消失（不回倉庫）。
- UI＝`drawRest` 重建 `restRects`（draw 階段註冊可點區域）；`restClick` 反向命中最上層。`drawLoadoutCard` 每員顯示**個性(§8 配槍前看覆蓋力)+武器+技能+彈藥±**；`drawRestWarehouse` 倉格(超上限紅框)+丟棄；`drawRestActionBar` 二選一/繼續；`drawRestOffers` 模態三選一。鍵盤＝便捷（CHOICE 1/2、OFFERS/TARGET 1‑9、DONE Enter）。

### 4.11 多張戰鬥地圖 + 地形（門 / 低矮牆 / 按鈕）
- **地圖 registry**：`MAPS.alpha`(原圖)/`MAPS.bravo`(門/掩體圖)；`let MAP` 為作用中地圖。衍生表(`N/adj/nodeVis/nextHop/EXTRACT/ENTRY/extractNodeId`)改由 `buildMapDerived(key)` 重建（原本是載入時 IIFE）。`startMission` 依 `roadNode(roadCurrent).map||'alpha'` 切換並 `for(door) open=false` 重置。ROADMAP COMBAT 節點加 `map` 欄。
- **門**：`MAP.doors=[{id,x1,y1,x2,y2,open}]`。關＝實體（擋移動/視線/子彈）；開＝無。視線/子彈經 `segBlocked`(牆+關門) 截斷；`nodeVis` 用 `clearLineWalls`(只看永久牆，與門開關無關、穩定)。
- **低矮牆**：`MAP.lowWalls=[{x1,y1,x2,y2}]`。**不擋視線**；玩家穿越減速(`lowWallSpeedMult`，控制移動與 `extractTick` 都套)；敵人視為實體（`collideWalls` 對 enemy 推開→靠 waypoint 邊繞行，**邊不得穿越低矮牆**＝地圖設計約束，已用無頭測試把關）；子彈＝`b.ox/oy` 發射原點到低矮牆 `<closeRange` 可越射、否則 `blockChance` 機率擋。
- **按鈕**：`MAP.buttons=[{id,x,y,doorId}]`，遠端連動門（showcase：bravo b1 開 door2 通往撤離）。
- **互動**：`tryInteract`（E 鍵；僅被附身單位）找 `interactRadius` 內最近門/按鈕→toggle。`nearestInteractable` 也供 `drawScreenOverlays` 顯示「E：開/關門/啟動」提示。**自走單位(撤離/閒置)不會開門**（§Q4）→ ⚠ 關門卡撤離路有 softlock 風險，故 bravo 設計讓門在側/中段、玩家推進時自然開好。
- 繪製：`drawDoors`(關=粗琥珀/開=淡虛線)、`drawLowWalls`(藍灰虛線)、`drawButtons`(連動門開=綠/關=琥珀)，並加進霧上 `drawSkeleton` 空間參考。

### 4.12 敵人狀態機（IDLE ↔ ENGAGED，latch）
- `enemy.state`：`IDLE`(守點被動：`enemyIdleTick` 只回正 `watchFacing`、不推進/不開火/不開門) ↔ `ENGAGED`(原選點/charge-hold/開火 + `enemyTryOpenDoors` 破門)。
- 進入 ENGAGED（**永不取消**）：① `enemyTick` 偵測 `canSee` 任一玩家 ② 玩家開火（`fireUnit` 內 `alertEnemiesNear`）`enemyHearRadius` 內、不論命中 ③ 被命中（`damageUnit` enemy 分支）。
- `makeEnemy` 預設 `state:'IDLE'`、`facing/watchFacing` 可由 `enemySpawn.facing` 指定（bravo 門後敵人面向 door＝預設 Math.PI 朝左）。**追兵(reinforce) 直接 ENGAGED**。
- bravo 玩法：開場敵人靜止守點、藏在關門後（門擋視線→霧中看不到），開門才現形/交火。

### 4.13 關卡編輯器（scene EDITOR）
- **進入/離開**：路線圖右上「✎ 關卡編輯器」鈕（`editorBtnRect`，MAP click 命中）→ `editorOpen`；編輯器內「離開」鈕或 Esc → `editorExit`(回 MAP)。`edit` = 編輯中地圖（同 MAPS 結構，**waypoints id===index**，刪點會 `removeWaypoint` 重編號並修正 edges/spawns）。
- **工具**(數字 1‑0 或工具列)：wall/door/low(拖拉線段 `editorCommitSeg`)、wp(點放)、edge(點兩 waypoint 連線 `toggleEdge`)、spawn(點 waypoint 循環 rusher→flanker→無 `cycleSpawn`)、button(放置並自動連最近門)、entry/extract(設點)、erase(就近刪/右鍵亦可)。`editTagBrush` 決定新 waypoint 的 tag。`editSnap`(G 鍵)格線吸附。
- **輸入**：`editorMouseDown`(先命中 `editRects` UI，否則畫布放置/起拖) / `editorMouseUp`(落筆拖拉線段)；座標 `snapPt`。
- **即時驗證** `editorValidate`：缺 waypoint/進撤點、edge 穿低矮牆數、進場↔撤離未連通——底部狀態列顯示，且擋掉不可玩的試玩。
- **試玩** `editorTestPlay`：把 `edit` 正規化存成 `MAPS.__editTest`，`forcedMap='__editTest'`+`fromEditor=true`，`startMission()` 直接打（不動到 run 的 roster/missionIndex/roadmap）；結束(`endMission` 首行)或 Backspace 中止 → 回 EDITOR。
- **輸出** `editorExport`：把 `edit` 序列化成可貼回 `MAPS{}` 的 JS 字面量 → `console.log` + `localStorage('sleeper_map')`；之後在 ROADMAP 某 COMBAT 節點加 `map:'鍵名'`。`editorLoad('blank'|'alpha'|'bravo'|'saved')` 可載入起點。
- 繪製 `drawEditor`/`drawEditGeom`/`drawEditorUI`/`eBtn`：渲染 edit 幾何(edge 穿矮牆顯紅)+工具列/標籤/動作鈕+拖拉預覽+吸附游標+驗證列。
- **Phase 3 擴充**：工具新增 `prisoner`(P 鍵/工具列「俘虜」)放置俘虜點；工具列加「時間:NNs」循環(0預設/60/90/120/150)設 `edit.timeBudget`；`editorExport` 帶出 `prisoners` 與（非 0 時）`timeBudget`。

### 4.14 裝備 / 道具（每單位各 1 槽，可未來擴充）
- `EQUIPMENT`（持續被動，`fx` 效果於 `makePlayer` 套用並存成 per-unit 修正）：maxHpAdd / dmgResist(減傷) / senseMul / reloadMul / magMul(彈夾) / spreadMul(發散) / turnMul / moveMul。現有 armor/scope/quickmag/extmag/stabil/light/plate。
- `ITEMS`（一次性，戰鬥按 **Q**＝`useItem`，用完 `itemUsed=true` 不再帶回）：`heal`(瞬補)、`refill`(補滿彈夾+備彈)、`buff{dur,moveMul,fireRateMul}`(腎上腺素短時)。buff 由 `tickAmmo` 倒數。現有 stim/ammobox/adrenaline。
- roster entry 加 `equipId`/`itemId`；`makePlayer` 衍生 `dmgResist/reloadMul/spreadMul/turnSpeed/moveMul/magazine/buffT/buffMove/buffFire`；消費端：`damageUnit`(減傷)、`startReload/tickAmmo`(`unitMag`/reloadMul)、`fireUnit`(spreadMul/buffFire)、移動(moveMul/buffMove)、turn(turnSpeed)。
- 進共用倉庫、佔格（同武器/技能 cap 規則）。搜刮池(`rollScavenge`)、軍械庫(`rollArmory`) 皆可出。配裝 UI＝休息關 loadout card 新增 裝備/道具 兩列（`equipGearFromWarehouse`/`unequipGear`，通用 kind/field）。戰鬥卡右上顯示 🛡裝備 / Q道具。
- **裝備加的 maxHp 不計入 `bonusMaxHp`**（`rosterFromPlayer` 扣掉 `fx.maxHpAdd`），避免卸裝後血上限殘留。

### 4.15 軍械庫節點 / 俘虜 / 各關時間
- **軍械庫**（ROADMAP type `ARMORY`，node 2/8）：`enterArmory`→`rollArmory` 三選一(武器/裝備/技能/道具)→`armoryPick` 入倉庫→`advanceNode`。獨立 scene `ARMORY`（`drawArmory`，重用 `restRects`/`restClick`；鍵盤 1‑9）。不進戰鬥、不耗時。
- **俘虜**（任務內目標物）：`MAP.prisoners=[{x,y}]`，`startMission` 每場重置成 `prisoners=[{pos,freed}]`。`tryInteract`(E) 也釋放 `interactRadius` 內俘虜→`freePrisoner`：以 `makePlayer`(隨機 archetype + 底線 pistol、state IDLE) push 進 `players`；之後它像一般我方需自行撤離，**撤離成功 → `endMission` 的 survivors→roster 自然 +1**；陣亡就沒了。`drawPrisoners` 世界層青色柵欄記號（受迷霧/門遮蔽）。bravo 右區門後放 1 名示範。
- **各關時間**：`curTimeBudget = node.timeBudget || MAP.timeBudget || CONFIG.timeBudget`（`startMission` 設定）；軟計時/HUD 改讀 `curTimeBudget`。

### 4.16 敵人類型 / 裝甲 / 穿甲（ENEMY_TYPES）
- **資料驅動**：`ENEMY_TYPES` 目錄（位置在 ARCHETYPES 之後），欄位 `weaponId/hp/moveSpeed/personality/hue/radius` + 選填 `coneHalfDeg/senseMul/armor/attack`。`enemySpawns` 加選填 `type`（**預設 grunt**＝舊行為，舊地圖零改動）；`makeEnemy(nodeId, personality, isReinforce, facing, typeId)` 讀類型（spawn 未給 personality → 用類型預設）。grunt 的 hp/moveSpeed 仍引用 `CONFIG.enemy`，舊旋鈕續有效。
- **四型**：`grunt`(步兵, carbine)、`dog`(警犬: fangs, hp45, 速165, radius9, coneHalfDeg80, senseMul1.5, 無甲)、`heavy_lmg`(重機槍兵: lmg, 速55, 帶甲, `attack:'burst'`, personality `holder`)、`heavy_shot`(霰彈兵: shotgun, 速70, 帶甲)。
- **裝甲＝機率整發擋下**（非舊的固定減傷）：`damageUnit(u,dmg,pierce)` 第 3 參；enemy 分支先轉 ENGAGED（**即使擋下也交戰**），再判 `u.armor>0 && rand < armor×(1−pierce)` → `spawnBlock`(藍白「鏘」火花)、`return` 不扣血。`armor` 值＝該型 blockChance（兩重裝＝`CONFIG.armor.blockChance` 0.85），無甲＝0。有效擋格率 = blockChance×(1−pierce)。
- **穿甲 `pierce`（武器欄，0~1）**：pistol/smg/pdw/carbine=0、rifle=0.5、dmr=0.9、敵槍 fangs/lmg/shotgun=0。`fireUnit` 把 `W.pierce` 蓋到子彈 `b.pierce`，`updateBullets` 命中時 `damageUnit(t,b.dmg,b.pierce)`。近戰(警犬)走一般射擊管線，pierce=0。對 85% 裝甲：rifle 實際擋格 42%、dmr 8.5%、其餘 85%。
- **攻擊模組**（`enemyTick` ENGAGED 段呼叫 `enemyAttack(e,ang,dt)`）：`ranged`＝持續射擊（霰彈靠武器 `pellets:7`，`fireUnit` 迴圈一次擊發多顆、單次只耗 1 發）；`burst`＝重機槍點放（武器 `burst:{size8,gap0.7}`，打滿 size 發→`burstCd` 停 gap 秒讓 bloom 回復＝掃射節奏，沿用單位 `burstLeft/burstCd` 欄）。**警犬不另開近戰系統**＝fangs(range34/fireRate8) 走一般 ranged，靠 charge 衝到貼臉後連咬（使用者指定）。
- **視錐逐單位**：`canSee` 改讀 `observer.coneHalfDeg || CONFIG.vision.coneHalfDeg`（警犬 80°半角＝160°廣角）；`senseRadius` 本就逐單位（×senseMul）。迷霧繪製只畫被附身玩家自己的錐，不受影響。
- **掉落過濾**：`spawnDrop` 只掉 `LOOT_WEAPON_IDS` 內的武器，敵專屬 fangs/lmg/shotgun 不掉落（不入玩家戰利品池）。
- **演出期可見性豁免**：`drawEnemies` 的 isRevealed 過濾在 `cutscene.lock` 時略過（交給聚光迷霧）——否則第0關主角走遠時同事(enemy)會被收掉。
- **陣亡叉叉**：`killUnit` push `corpses[{x,y,enemy}]`（敵紅/我藍叉叉，`drawCorpses` 世界層繪、會被迷霧蓋、整場保留；startMission/startIntro 重置）。
- **繪製**：`drawEnemies` 用 `e.type.hue` 著色（dog 琥珀45/lmg 紫280/shot 藍200/追兵橙18）；帶甲者外加一圈金屬色板甲環。
- **編輯器**：spawn 工具循環改 步兵R→步兵F→警犬→重機槍→霰彈兵→無（`SPAWN_CYCLE`/`cycleSpawn` 以內容反查索引、重建以清舊欄位，不污染輸出）；`spawnInfo` 給節點記號短標(R/F/犬/機/霰)與顏色。`editorExport` 透過 JSON.stringify 自動帶出 `type`。

### 4.17 側翼 / 突襲（繞背加傷 + 破甲；敵我皆適用）
- **判定**：`damageUnit(u,dmg,pierce,fromAng)` 第 4 參 `fromAng`＝攻擊來向（目標→發射原點角度），由 `updateBullets` 命中時 `angTo(t.pos,{ox,oy})` 算出。`flanked = |angDiff(fromAng, u.facing)| > u.coneHalfDeg`（落在目標視野錐外＝側後方）。`surprise = (enemy && state==='IDLE')`（**在轉 ENGAGED 前判定**；僅對敵）。
- **倍率**：`mult = flanked ? flankMult(2.0) : surprise ? surpriseMult(1.5) : 1`——**取較高、不相乘**（繞背一個閒置敵也是 ×2）。
- **破甲**：`flanked` 時跳過裝甲擋格 roll **且** `dmgResist` 視為 0——**甲只罩正面**。因此對重裝有兩解：正面用穿甲武器(pierce)，或繞背任何槍。**雙向對稱**：敵人繞背我方一樣破我方 `dmgResist` 並 ×2（我方裝備 armor/plate 自此變成正面防護）。
- **回饋**：繞背/突襲命中＝`spawnCrit`（紅色重擊火花，與一般 `spawnHit` 黃、`spawnBlock` 藍白 區分）。無傷害數字，靠粒子讀。
- **互動**：與 pierce 獨立（pierce 改擋格機率、flank 整個跳過擋格+加傷）。ENGAGED 敵會持續轉向面對最近我方→單人難繞背，逼用「第二單位拉注意力」＝與附身小隊玩法同向。`senseRadius`(360°近身感知) 不參與（甲是物理朝向，非察覺）。CONFIG：`flank{flankMult2.0, surpriseMult1.5}`。

### 4.18 第0關（序章 / 教學）+ cutscene 導演系統
- **進入**：`initRun()` 不再直接進路線圖，而是 `startIntro()`——`intro` 地圖、scene `COMBAT`、`introActive=true`、建 `cutscene` 導演。每局都播。
- **導演 `cutscene`**（可重用）：`{ beats[], i, line, subtitle, lock, gate, cam, victim, flags, advance }`，由 `cutsceneTick(dt)` 推進（while 迴圈把 instant 拍同幀連續處理）。節拍 `kind`：`say`(對白框，點擊/空白續)、`move`(actor lerp 到點)、`face`、`wait`、`cam`(固定機位 {x,y,angle} / `'follow'`→null)、`sub`(持續字幕)、`do`(副作用 fn)、`shoot`(等左鍵→處決)、`gate`(解鎖玩法等事件)。`beatEnter/beatDone/gateSatisfied` 三函式驅動。
- **update/draw 接管**：`update` 內 `if (cutscene) cutsceneTick(dt); updateCamera; if (cutscene.lock) return;`（lock 拍只跑鏡頭/演出、凍結 sim；gate 拍 lock=false→常規操作照跑、導演檢查事件）。`draw` 加 `drawIntroActors`(守衛/控制台) + `drawCutscene`(對話泡+字幕+Esc提示)；**lock 時隱藏 HUD/卡片/螢幕標記**＝乾淨電影感。`updateCamera` 新增 `cutscene.cam` 固定機位分支。
- **聚光迷霧**：lock 拍時 `drawFog` 改成「只挖 `cutscene.reveal`（羽化圓）」＝重點演出區外全包迷霧；主角去洗手間＝走出聚光圈沒入迷霧、再走回。`reveal` 由 `reveal` 節拍切換（開場管制室→守衛戲移到廊道）。gate 拍 lock=false 自動回一般玩家視野迷霧。
- **對話泡（取代底部對話框）**：`say` 節拍帶 `pos`(說話者世界座標 fn)；`drawSpeechBubble` 在說話者頭頂畫圓角泡+小尾、**不顯姓名**、`wrapText` 自動換行、超出畫面改放下方。舊版 `drawDialogueBoxLegacy`(底部框+姓名) 保留備用、不再呼叫。
- **流程六拍對到教學**：對白(管制室)→主角往返→`shoot`(左鍵處決同僚＝enemy/IDLE/被貼背繞背爆擊→教**開火**)→`cam follow`接手→`gate codes`(E 取得密碼，`introConsole` 互動點，教**WASD+E**)→`gate door`(E 開 `exit` 門)→守衛現身(`introGuards` actor)密語→`guardsJoin`(比照俘虜但帶 RECRUIT 槍，用 `introCast[1/2]`)→`gate extract`(教 **H/Shift+H**)。
- **陣容/結束**：`introCast`＝主角(pistol)+2守衛(RECRUIT槍)。撤離→`endMission(introActive)`→`finishIntro()`：roster=撤出者(=introCast)、`enterRest(true)`(序章休息含教學面板)。`restDoContinue` 以 **`roadCurrent==null`** 判別序章休息→進路線圖(否則 advanceNode)。
- **Esc 跳過**：`skipIntro()` 只跳演出——roster 直接用 introCast、`enterRest(false)`(不顯教學)，照樣進休息選行動。keydown `introActive && Esc` 觸發（在 cutscene-lock 分支之前，任何時候可跳）。
- **互動/輸入**：`nearestInteractable`/`tryInteract` 加 `console` 型（E 取得密碼）。keydown 有 cutscene-lock 分支（只收對白續播/Esc，封鎖其餘）；mousedown lock 時：有對白→左鍵續播、無對白(shoot拍)→放行 mouse.down 給 shoot 讀。`restTutorial` 旗標控制休息教學面板（`drawRestTutorial` 停靠左上）。

### 4.19 潛行 / 警覺層（Stage 1：識別空窗 + IDLE 短錐 + overwatch 收火）
為「攻克並通過哨點」打底（潛行層分 5 階段，此為地基）。設計討論結論：**不做調查狀態、改識別空窗；ENGAGED 永鎖（無增援，唯一增援＝時間到從入口湧入）**。
- **`canSee` 依 observer 狀態**：idle 敵人＝窄錐(`coneHalfDeg×idleConeMul`)＋**有限視距 `idleViewRange`**（超出只剩 senseRadius 近身）；engaged/玩家＝全錐全距。是「能繞過哨兵」的盲區來源。
- **識別空窗（取代瞬間交戰）**：`makeEnemy` 加 `detect`(0~1)。`enemyTick` 未交戰時：看到玩家 → `detect += rate·dt`（`rate=1/identifyTime`，貼臉 ≤senseRadius ×`distNearMul` 近乎瞬識、否則 `clamp(1.4−d/idleViewRange,.3,1.4)` 近快遠慢、遮蔽 ×`concealMul`(Stage 2 hook `inConceal`)）；看不到則 `−decay·dt` 衰退。`detect≥1` → ENGAGED(永鎖)。**快速閃過＝累不滿＝安全**。**槍聲(`alertEnemiesNear`)/被擊中(`damageUnit`) 仍瞬間 ENGAGED，繞過空窗**。
- **overwatch 對 IDLE 敵收火**：`idleTick`(我方閒置/撤離 overwatch)目標篩選加 `e.state==='ENGAGED'`——不對未警覺敵自動開火＝不破壞潛行；**被附身單位手動開火不受限**。
- **識別回饋（公平性命脈）**：`drawEnemies` 對 `detect>0 且未 ENGAGED` 的敵畫頭頂漸滿弧（黃→紅）+ `?`→`！`。
- CONFIG：`stealth{ idleConeMul0.6, idleViewRange360, identifyTime0.8, distNearMul6, concealMul0.25, decay1.5 }`。
- **Stage 2 遮蔽區（草叢）**：`MAP.bushes=[{x,y,w,h}]`；`inBush`/`inConceal`；`canSee` 加 MOBA 草叢——目標在叢、觀察者不在同叢且超出 senseRadius → 看不到（同叢互見、貼臉保底）。**`drawEnemies` 改依 `isRevealed` 才繪**（叢內敵未被感知＝不畫；靠近/同叢才現形）。`drawBushes` 半透明綠地塊；我方在叢內畫虛線綠圈（隱蔽中）。識別速率在叢內 ×`concealMul`（enemyTick 的 `inConceal` hook 生效）。編輯器加 `bush` 工具(B 鍵/工具列「草叢」,拖拉矩形)、export/normalize/erase 全含 bushes。
- **Stage 3 消音手槍 + 亞音速彈**：`WEAPONS.silpistol`（`silent:true`、高單發40＝繞背×2 一槍斃 70hp、短程、彈夾6）。`fireUnit`：silent → **不呼叫 `alertEnemiesNear`**（不發槍聲）、小槍口、`Sfx('silshot')`。彈藥稀少＝`CONFIG.ammo.subsonicStart(18)`（`initRun` 給 silent 武器此值；搜刮「補給」不補亞音速彈，維持稀少）。入 `LOOT_WEAPON_IDS`。
- **Stage 4 望遠（右鍵按住）**：`CONFIG.scope{anchorY0.82,scale0.86,spreadMul0.5,toggle:false,sensMul:1}`。`isScoping()`(右鍵 held＋控制中)→ `updateCamera` lerp `camAnchorY`/`cam.scale` 往前帶（`applyCam`/`w2s` 改用 live `camAnchorY`）；`manualFire` 發散 ×spreadMul 穩定；移動禁奔跑(`!isScoping()`)；瞄準示意線拉長到武器射程。**技能改只空白鍵**（右鍵不再放技能）。`toggle`/`sensMul` 為未來 option 留鉤子。
- **Stage 5 前哨站 `outpost`**：兩道隔牆＝兩處 choke 哨點，守兵 `facing:Math.PI` 面向進場側；4 塊草叢供繞路/消音點名；choke2 放 heavy_lmg(帶甲逼繞背)。roadmap node5 `map:'outpost'`（綠色 hue110 + 圖例「前哨站」）。**潛行＝別讓任一哨兵識別條累滿**。

### 4.20 可破壞地形（destructibles）
一塊地形有 HP + **intact/broken 兩形態**，被「擋下」的子彈扣 HP、歸零換形態。資料驅動：`MAP.destructibles=[{x1,y1,x2,y2,hp,intact,broken}]`（線段，runtime 加 `cur`/`curHp`，`startMission`/`startIntro` 重置）。
- **形態輪廓表 `DESTRUCT`**（move:true全擋/'slow'減速可越/false通；los;bullet:'block'必擋/'glass'非穿甲擋穿甲穿/'prob'機率擋(沿用低矮牆)/'pass'穿不壞）：`wall`(擋擋擋)、`glass`(擋·通·glass)、`lowwall`(slow·通·prob)、`fence`(擋·通·pass)、`flat`(全通)。`dProfile(d)=DESTRUCT[d.cur]`。
- **預設（你的四種＋鐵網）**：木箱 lowwall→flat、落地窗 glass→flat、有矮牆窗 glass→lowwall、可破壞全遮蔽 wall→lowwall、鐵絲網 fence(看穿/穿彈/打不壞)。
- **接線**：`segBlocked`(los 的才擋視線＝wall)、`collideWalls`(move===true 全擋、'slow' 僅擋敵)、`onLowWall`(slow 形態也減速玩家)、`updateBullets`(依 bullet 輪廓判擋/扣HP/換形態；玻璃 `blocked=rand<1×(1−pierce)`＝非穿甲必擋穿甲必穿，穿/擋都扣HP；**敵我子彈都算**)。動態、不進 `nodeVis`（比照門）。
- **繪製** `drawDestructibles`（wall 棕/glass 淡藍透明/lowwall 棕虛線/fence 交叉網；受損變淡）。
- **編輯器**：`destruct` 工具(C 鍵/工具列「破壞」拖線段)+預設循環(V 鍵/「破壞:名稱」按鈕)；export/normalize/erase/validate 全含 destructibles；`editorValidate` 把「擋移動的破壞物壓 waypoint 邊」也算進卡牆檢查。
- **示範**：alpha 放了四型各一（皆放 grid 格內不壓邊）。延後：爆裂桶(AoE)、可射熄燈(動態視野)。

### 4.21 數值平衡面板（即時調參，DOM 覆蓋層）
按 **`` ` ``（反引號）或右上齒輪** 開關的 DOM 側欄，直接寫入 `CONFIG`/`WEAPONS`/`ENEMY_TYPES`/`ABILITIES`/`EQUIPMENT`/`ITEMS` 的**活物件**，故多數即時生效（武器 `u.weapon` 是共用參考＝即時；唯彈夾大小、`unitHp`、敵人 hp/armor 於下次生成/換彈套用）。
- **兩層分離（為了 headless 可測）**：① 資料層純函式——`TUNE_ROOTS`(六個目錄)、`TUNE_DEFAULTS`(載入期深拷貝快照，供「重設」)、`tuneOverrides`(扁平 `"ROOT.sub.key"→值`，只存與預設不同者)、`tuneResolve/tuneSet/tuneGet/tuneDefaultAt/tuneRecordChange/applyTunerSaved/tunerExportText/restoreFromDefault/tunerReset`，全程不碰 DOM；② UI 層 `initTunerUI/buildTunerBody/tuneMakeNumRow/tuneMakeBoolRow/applyTunerFilter/toggleTuner`，**整包 try/catch**，無 DOM 時 `tunerEl=null`、`toggleTuner` 變 no-op。
- **自動生成**：`tuneBuildObj` 遞迴走物件——數字→滑桿(範圍依預設值自動推算 min/max/step)+數字輸入框、布林→勾選、字串/陣列/函式略過、子物件→`<details>` 巢狀。**新增 CONFIG 數值不必改面板**。四分頁：核心(CONFIG)/武器/敵人/技能·裝備·道具，附搜尋過濾。
- **持久化**：變更即寫 `localStorage['sleeper_tuning_v1']`，載入期 `applyTunerSaved()` 套回（重整保留）。「輸出覆寫」吐可貼回 index.html 的 `path = value;` 清單；「全部重設」還原快照並清空覆寫。
- **接線**：`keydown` 最前面加（a）聚焦於 INPUT/TEXTAREA/SELECT 時直接 return（不讓打字觸發遊戲鍵）、（b）`` ` `` 切換面板；`keyup` 同樣加聚焦守衛。開面板時 `exitPointerLock`（戰鬥因此暫停＝可邊看邊調，點畫面重新鎖定續跑）。

### 4.22 獨立地圖編輯器 `editor.html`（新工具，與 index 分檔）
**獨立自足單檔**的 DOM 地圖編輯器（瀏覽器開 `editor.html` 即用），與遊戲 `MAPS` **同結構、可雙向 JSON round-trip**。使用者核可：獨立檔 / v1 不做試玩 / 先做編輯核心。原 in-index 編輯器(§4.13)保留並存。完整規劃見 `地圖編輯器規劃.md`。
- **重點踩雷**：遊戲以 `wp = id => MAP.waypoints[id]`＝**陣列索引當 id**，故編輯器**刪 waypoint 必須重編號**（`removeWaypoint` splice 後重設 id、過濾/位移 edges 與 enemySpawns 的 nodeId）。否則匯出的地圖 nodeId 會錯位。
- **畫布**：平移(中鍵/空白+拖)、縮放(滾輪、繞游標)、格線吸附、fit-to-view；世界仍 1280×720。
- **三分頁共用同一畫布/模型**，切頁＝切「可編輯層＋屬性面板」、他層淡化：① 地圖物件(牆/門/低矮牆/破壞物5預設/草叢/按鈕連動門/進撤點；拖曳放置、選取拖移整體或端點、屬性面板改精確座標) ② waypoint＋連線(加/拖/刪點；連線工具點兩點建/斷、點線上刪邊、rubber-band 預覽；節點檢視座標/連線數/標籤勾選；**邊穿牆/低矮牆/擋移動破壞物即時紅標**＝AI 卡牆防呆) ③ 敵人配置(點 waypoint 指派 type/personality/facing，**朝向把手＋視野錐預覽**；俘虜放置)。
- **undo/redo**(JSON snapshot stack)、即時驗證列(缺進/撤點、孤立節點、邊穿牆、按鈕 doorId 失聯)。
- **匯出/匯入**：純 JSON(可再匯入、round-trip 穩定) ／ 「MAPS 片段」(`key: {…}` 貼回 index.html `const MAPS`)。內嵌 alpha/bravo/outpost/lanes 快照為可編輯範本。
- **延後**(使用者同意)：具名 localStorage 草稿管理、檔案下載/上傳、試玩(index `?test=1` 接縫)。
- **同步注意**：editor.html 自帶一份小常數(ENEMY_TYPE_IDS/ENEMY_META 視野錐/PERSONALITIES/WP_TAGS/DESTRUCT_PRESETS/FEATURES/TEMPLATES)；index.html 改這些時需手動同步到 editor.html。

**v2 追加（地圖特徵 + 群組 + 重疊）**：
- **特徵庫 `FEATURES`**（Tab1「★特徵」工具，拖框放置）：柱子/簡易房間(缺口)/房間+真門/長廊(水平垂直)/轉角(四角)/窗牆(牆+玻璃)/掩體叢(3 低矮牆)/十字路口。`stampFeature(idx,box)` 呼叫 `feat.build(box,opt)` 產出**扁平基本物件**(rectWalls 等)推入各陣列，回傳建立的 refs 設為當前群組(放下即可整組調)。子選項用 `featureOpt`(門側/方向/角落)。
- **群組＝框選的選取集 `selRefs`**（**不存持久 `_gid`，格式仍與遊戲一致**）：選取工具空白拖曳＝`marquee` 框選(bbox 相交)→ `selRefs`；外框 8 控制點 `GHANDLES` 縮放(`groupScale`，對側錨點、邊單軸/角雙軸、`mouseShift` 等比、最小 0.05)；外框內部拖曳＝整組位移(reset 到 `drag.base` 再平移＝不累積)；`drawGroupBox` 畫外框+控制點+成員框。變換套用 `moveRef/scaleRefFrom`(線段端點/點/草叢 x,y,w,h)。
- **重疊放置**：各工具一律新建不擋既有物件(本就允許)；新增**重疊點擊循環** `hitObjectsCycle`(同點重複點選堆疊下一個，用 `lastPick`)。
- **門切牆缺口**：`cutWallsForDoor(door)`——門與某牆共線(點到線<2px)且投影重疊→把牆從門兩端 t0/t1 切成最多兩段、移除中段。門工具放置 / 房間+真門 / 門 inspector「切出牆缺口」鈕觸發。
- headless v2 驗證 28 條（8 特徵 build 數量、門切牆 3 例、群組 bbox/位移不累積/縮放、重疊 hitAll+循環、marquee、round-trip 無 `_*` 殘留、三分頁+群組/特徵預覽 render）。

**v2 追加（參考底圖 underlay）**：第 4 分頁「④ 底圖」。載入一張真實街區/平面圖墊在地圖**下方**供描圖；**完全 editor-only、不進 `map`、不影響匯出匯入**。狀態 `underlay={img,src,x,y,scale,opacity,visible,w,h}`，存自己的 localStorage 鍵 `sleeper_editor_underlay`（含 dataURL，配額不足則 try/catch 略過）。所有分頁都會在世界背景之上、其餘之下渲染底圖（`drawImage`，套 `opacity`）；僅「底圖」分頁可操作：拖移(`umove`)、拖四角等比縮放(`underlayScale`，錨定對角)、**旋轉**(上方把手 `urotate`，Shift 吸 15°；`rot` 弧度，繞中心)。**旋轉感知**的命中測試：`uInside`/`uHandleWorld`/`uRotHandleWorld` 以中心反旋轉游標判定，縮放在任意角度仍錨定對角。inspector 有載入/x/y/縮放%/旋轉°/透明度/顯示/清除。headless 驗證（匯出無 underlay/rot、縮放錨定、位移、旋轉把手/拖曳、persist 隔離、四分頁 render、清除）。

**v2 追加（地圖尺寸 + 自動連線）**：
- **地圖尺寸可調**：地圖物件新增 `width`/`height` 真欄位（預設 1280×720；`normalize` 缺值時由 `contentExtent` 內容範圍推估，舊地圖照常）。**遊戲本就支援任意大小**：相機跟隨單位、不夾 WORLD；`WORLD` 常數只供迷霧/`MAXD`(AI rush 正規化，超出即飽和，無害)。編輯器世界框/格線/fitView 改讀 `map.width/height`；標題列「尺寸 寬×高」輸入即時調整＋「外牆」鈕(`rebuildBoundary` 依尺寸重建 inset40 邊界、`findBoundaryWalls` 先移除舊框)。width/height 會被匯出(遊戲忽略未知欄位＝無害)。
- **新增 waypoint 自動連線**：waypoints 分頁「自動連線:開/關」(`autoLink`)。開啟時用 `add` 工具新增點 → `autoLinkWaypoint` 連到所有「中間無阻隔(牆/低矮牆/擋移動破壞物，沿用 `moveBlockers`)」的既有點；node inspector 另有「自動連線此點」鈕。先把格局拉好再連續灑點即可大致成圖。
- headless 16 條（尺寸欄位/內容推估/round-trip、外牆重建依尺寸且替換不重複、自動連線連通/被牆/低矮牆擋下/add 工具整合、大地圖 render）。

---

## 5. 資料模型與目前內容（精確）

```
RosterEntry { name, hue, strength, weaponId, abilityId(可null), archetypeId, bonusMaxHp, hp,
              allocatedAmmo, disciplineOverride(null|'TAP'), equipId(可null), itemId(可null) }
Weapon  { name, rarity, weight, damage, fireRate, range, projSpeed,
          spreadBase, spreadPerShot, spreadMax, spreadRecover,
          magazine, reloadTime, turnSpeed,
          pierce(0~1穿甲), pellets?(霰彈), burst?:{size,gap}(點放), silent?(消音：不發槍聲+亞音速彈稀少) }   // reserveAmmo 已移除 → 改共用 ammoStock
Enemy  增 { detect(0~1 識別進度) }   // 潛行識別空窗
MapDef 增 { bushes?:[{x,y,w,h}](草叢), destructibles?:[{x1,y1,x2,y2,hp,intact,broken}](可破壞地形) }
Destructible 形態 ∈ wall/glass/lowwall/fence/flat（runtime 加 cur/curHp）
EnemyType { name, weaponId, hp, moveSpeed, personality, hue, radius,
            coneHalfDeg?, senseMul?, armor?(=blockChance,>0即帶甲), attack?('ranged'|'burst') }
Equipment { name, rarity, fx:{maxHpAdd?,dmgResist?,senseMul?,reloadMul?,magMul?,spreadMul?,turnMul?,moveMul?} }  // 持續被動
ItemDef  { name, rarity, heal?|refill?|buff:{dur,moveMul,fireRateMul} }  // 一次性道具
Item    { kind:'weapon'|'ability'|'equip'|'item', id }   // 倉庫元素（4 類）
Drop    { pos, kind:'weapon', id, ammo }       // 地上掉落（ammo＝休息關折算進儲備的量）
Door    { id, x1,y1,x2,y2, open }              // 關＝實體牆、開＝無
LowWall { x1,y1,x2,y2 }                        // 掩體：不擋視線、玩家減速、敵實體、子彈機率擋
Button  { id, x,y, doorId }                    // 遠端連動門
Prisoner{ x,y }(map定義) → {pos,freed}(runtime)  // 任務內目標物：E 釋放→入我方
Enemy   { ..., state:'IDLE'|'ENGAGED', watchFacing,
          typeId, type, moveSpeed, coneHalfDeg, armor, attack, burstLeft, burstCd }  // 純狀態機(latch) + 類型化
Bullet  { x,y,vx,vy,dmg,faction,range,traveled, ox,oy, pierce }  // ox/oy＝發射原點(低矮牆越射判定); pierce＝穿甲
EnemySpawn 增 { type?(預設grunt), personality?, facing? }
MapDef  增 { label, timeBudget?(0/省略=用全域), prisoners?:[{x,y}] }
RoadNode 增 { type:'COMBAT'|'REST'|'ARMORY', map?, timeBudget? }
```

**WEAPONS**（weight 影響奔跑；turnSpeed＝迴轉速度；彈藥改共用儲備 `ammoStock[weaponId]`）
- `pistol` common w0.5 dmg10 fr6 rng280 proj680 spread2.5/2/12/10 mag12 reload1.1 turn600 — **底線武器(§7)，彈藥無限、不掉落、不入儲備**
- `smg`  common w0.9 dmg13 fr12 rng330 proj740 spread3.2/2.6/16/11 mag35 reload1.3 turn560
- `pdw`  uncommon w1.2 dmg16 fr9 rng380 proj780 spread2.2/2.2/13/9 mag40 reload1.5 turn520
- `rifle`common w1.8 dmg22 fr7 rng470 proj840 spread1.4/1.8/10/8 mag30 reload1.8 turn440
- `carbine` common w1.3 dmg9 fr3 rng340 proj640 spread3/1.8/14/8 mag30 reload2.0 turn360 **pierce0** — **敵人武器（已入目錄、可掉落/撿/裝備）**
- `dmr` rare w2.0 dmg34 fr3.5 rng560 proj980 spread1/1.4/8/7 mag15 reload2.0 turn400 **pierce0.9** — 搜刮天花板、裝甲可靠解
- **穿甲 pierce**：pistol/smg/pdw/carbine=0、`rifle`=0.5、`dmr`=0.9（對 85% 裝甲實際擋格率＝85%/85%/85%/85% · 42% · 8.5%）。
- **敵專屬武器（不入玩家戰利品池、不掉落）**：`fangs`(警犬 dmg7 fr8 rng34 連咬)、`lmg`(重機槍 dmg11 fr11 rng420 mag60 `burst{8,0.7}`)、`shotgun`(霰彈 dmg6 fr1.4 rng240 `pellets7`)。
- 招募只抽 `RECRUIT_WEAPON_IDS=[smg,pdw,rifle]`；掉落/搜刮戰利品池 `LOOT_WEAPON_IDS=[smg,pdw,rifle,carbine,dmr]`（rarity 加權）。

**跨關回血**：`rosterFromPlayer` 存撤離者當前 `hp`；`startMission`/`makePlayer` 開場血量 = `clamp(carried + (maxHp−carried)×CONFIG.interMissionHealFrac, 1, maxHp)`。預設 `interMissionHealFrac=1.0` ＝每關自動全補（與舊行為完全相同、無外顯改變）；調低（0＝殘血照帶、0.5＝補一半缺口）即讓「帶傷進關」成為壓力。注意：目前 REST 只有 +maxHP/招募，**沒有真正的補血選項**——若把此鈕調低，REST 需要再加一個「治療」選項才完整（backlog）。新兵 `makeRecruit` 帶 `hp:unitHp`（滿血入隊）。

**ABILITIES**（單槽：每員 1 技能；倉庫存備品供休息關「換」非「疊」；`abilityId` 可為 null＝無技能）：`heal`(8s/+25)、`medkit`(14s/+50)、`firstaid`(5s/+14)。皆佔位＝瞬補生命、僅手動、超量夾 maxHp，空白/右鍵施放。關卡內回血只有此技能（與跨關回血旋鈕互補）。

**EQUIPMENT**（裝備槽 1，持續被動，見 §4.14）：armor(減傷.15)/scope(感知×1.4)/quickmag(換彈×.7)/extmag(彈夾×1.5)/stabil(發散×.7,rare)/light(迴轉×1.2移動×1.1)/plate(減傷.3移動×.9,rare)。
**ITEMS**（道具槽 1，一次性 Q 鍵，見 §4.14）：stim(瞬補60)/ammobox(補滿彈夾+備彈)/adrenaline(6s 移動×1.4射速×1.4)。

**共用倉庫 / 臨時背包 / 彈藥儲備**（全 run 層級，`initRun` 重置）：
- `warehouse [Item]`：未裝備的武器/技能。容量 `warehouseCap()=roster.length×slotsPerMember(2)`；`warehouseUsed()` 視 `abilitiesCountTowardCap` 是否計入技能；`overCap()` 為門檻（喚醒與繼續都被它擋）。已裝備在單位身上的不佔格。
- `backpack [Item+ammo]`：任務中撿到、任務中不可動用；`enterRest` 一次處理（武器折算彈藥進儲備＋item 進 `warehouse`，技能直接進 warehouse），清空。
- `ammoStock {weaponId:number}`：每武器類別共用儲備（`stockStart` 寬裕、本階段不咬人）。休息關 `allocAmmo(mi,±)` 在儲備↔成員 `allocatedAmmo` 間搬移（步進＝該槍 magazine）；換武器類別時舊型彈退回儲備、新型歸零。pistol 不納入。

**ARCHETYPES**：見 §4.5（8 種）。

**ENEMY_TYPES**（敵人類型，見 §4.16）：`grunt`(步兵/carbine)、`dog`(警犬/fangs/快脆廣視錐/連咬)、`heavy_lmg`(重機槍兵/lmg/帶甲/burst/holder)、`heavy_shot`(霰彈兵/shotgun/帶甲/推進)。`CONFIG.ai` 新增 `holder`(守點壓制：低 wRush、高 wExpose)。

**招募隨機**：`makeRecruit` 從 `UNIT_NAMES`(18 個)、`HUE_POOL`(11 色)、strength∈{0.8,1.0,1.2}、`WEAPON_IDS`、`ARCHETYPE_IDS` 均勻抽。`baseRoster()` 開局抽 `squadSize`(3) 人；`restAwaken()` 抽 1 人。**起始陣容與喚醒全隨機、無固定基準**（使用者明確要求）。

**地圖 `MAPS`**（`buildMapDerived(key)` 切換作用中 `MAP` 並重建衍生表）。**2026-06-22 地圖重設計**：舊圖都是「整道隔牆＋單一缺口」逼擠同一洞，側翼/潛行無從施展 → 改成開放+掩體島+草叢+多路線。**舊片段備份於 `舊地圖備份.md`**。設計原則：waypoint 走 3×3 grid（欄 130/340/560/760/960/1140 × 列 180/360/540），掩體島嚴格放在格子內(不壓 edge 線)＝邊永不穿牆（無頭幾何測試把關）。
- `alpha`＝**開闊庭院**（取代原「開闊區」）：**無橫貫隔牆**，6 座掩體島(含中央)+4 草叢+14 waypoint 路網；4 哨兵(node5/8/7 步兵+node12 霰彈兵)皆 `facing:Math.PI` 面西。**可上/下路繞到哨兵側後(繞背×2)、可叢間潛行**。
- `bravo`（門/掩體圖，**保留**＝破門強攻原型）：兩道隔牆+door1/door2+低矮牆+按鈕；含警犬/重機槍/霰彈兵；**開門才見敵**。
- `outpost`＝**前哨站 v2**（取代第一版）：兩處哨點改「**中央短牆(y300-420)＋上下繞道**」，不再整道隔牆；4 草叢；CP2 放 heavy_lmg。**可繞上/下到哨兵側後**。
- `lanes`＝**雙線哨廊**（新）：兩道**平行低矮牆**(看得過去/可越)分三道，缺口對齊連接點 x340/560/760/960＝**可換道繞到哨兵側後**；中道 heavy_lmg。
- `intro`：第0關序章（§4.18）。進場側固定左、撤離右。ROADMAP COMBAT 節點 `map` 欄指定（預設 alpha；node1/6/9＝bravo、node4＝lanes、node5＝outpost）。`drawRoadmap` hue：開闊8/門禁35/前哨110/哨廊190/軍械48/休息150 + 圖例。

---

## 6. CONFIG 完整對照（目前值）

```
switchHandoverMs 0 · idleAccuracyMult 0.5 · idleReactionDelay 0.4 · idleFireRateMult 0.7 · idleTap{size3,gap0.55}
unitHp 100 · moveSpeed 110 · turnSpeed 480(預設) · mouseSensitivity 0.0035
sprint{base1.9,weightPenalty0.5,minMult1.1}
timeBudget 90 · reinforce{start2.0,rampEvery10,rampDelta-0.3,min0.4} · hysteresisBonus 25 · squadSize 3 · interMissionHealFrac 1.0
loadout{slotsPerMember2,abilitiesCountTowardCap true,scavengeChoices3} · ammo{stockStart600,defaultAlloc240,salvageAlly120,salvageEnemy60} · pickupRadius 26
unitRadius 12 · nodeReach 22 · extractRadius 34 · sweepAmpDeg 70 · sweepSpeed 35 · wanderAmpDeg 28 · wanderSpeed 45
ghostAlertTime 4.0 · ghostCloseRange 150 · lowHpFrac 0.35
vision{coneHalfDeg45,senseRadius90,fanRays28,coneMax2200} · fog{color#05070b,skeletonAlpha0.16}
camera{scale1.0,followLerp9,rotateLerp8,anchorY0.66}
enemy{moveSpeed85,hp70,reactionDelay0.35,strength1.0}（grunt 用此；其餘類型見 ENEMY_TYPES）
interactRadius 40(門以線段最近點量距) · enemyHearRadius 320 · lowWall{slow0.3,touchR12,closeRange90,blockChance0.55}
armor{blockChance0.85}（重裝裝甲；有效擋格率＝blockChance×(1−pierce)）
flank{flankMult2.0, surpriseMult1.5}（繞背×2且破甲／閒置敵正面×1.5；取高不疊；敵我皆適用）
stealth{idleConeMul0.6, idleViewRange360, identifyTime0.8, distNearMul6, concealMul0.25, decay1.5}（潛行：IDLE 短錐+有限視距+識別空窗）
scope{anchorY0.82, scale0.86, spreadMul0.5, toggle:false, sensMul:1}（望遠：右鍵前帶+穩定+禁奔跑；toggle/sensMul 留 option 鉤子）
ammo 另加 subsonicStart18（消音武器 silent 的稀少亞音速彈）· camera.anchorY 改用 live camAnchorY（望遠 lerp）
chargeMul1.7 · holdProxMul0.2 · ai.rusher/flanker/holder(tag/wRush/wFlank/wExpose/wSight)
```

---

## 7. 操作

- **戰鬥**：點擊鎖定滑鼠開始 → 滑鼠移動=轉向、左鍵開火、**右鍵=望遠(偵察：鏡頭前帶+穩定+禁奔跑)**、**空白=技能**、WASD 移動、**Shift 奔跑**、R 換彈、**H 撤離(進入護送)**、**Shift+H 全體撤離**、1/2/3 附身(無CD)、G 除錯(關霧)、J hysteresis、Esc 暫停。撤離護送中：滑鼠轉視野不中斷，移動/開火/技能/換彈/道具/互動＝奪回控制。
- **路線圖**：點擊或數字鍵選發亮節點。
- **戰鬥（新增）**：E＝互動（開關門/按鈕/**釋放俘虜**，僅被附身單位）；**Q＝用道具**（一次性）；走過低矮牆會減速（掩體）；走過掉落物＝撿入背包。
- **軍械庫(ARMORY)**：點擊或數字鍵 1‑3 三選一 → 入倉庫。
- **休息**（主要用滑鼠點選；鍵盤為便捷）：CHOICE 階段 1=搜刮(三選一)、2=喚醒(倉庫需 ≤上限)；OFFERS/TARGET 1‑9 選；DONE Enter/空白 繼續。可隨時點倉庫物→點成員列裝備、▸倉卸下、±分配彈藥、丟棄選取。
- **結算**：R 或點擊重開一輪 run。
- **關卡編輯器**（路線圖右上鈕進入）：左鍵放置/拖拉、右鍵刪除、數字 1‑0 切工具、G 格線、Esc 離開；「試玩」直接打、Backspace 中止試玩；「輸出」把地圖印到 console + 存 localStorage，貼回 `MAPS{}` 並於 ROADMAP 節點加 `map:'鍵名'` 即成新關。

---

## 8. 設計決策與取捨（脈絡：為什麼這樣）

1. **待機可信但不足**：待機開火基礎較差（spreadBase/0.5）、慢射速、反應延遲、只看 90° 錐+近身；個性決定可靠度。放著不管終會被繞側折掉、單靠待機打不贏整圖。
2. **chargeMul/holdProxMul（超出原規格的敵 AI 旋鈕）**：依「敵人能否 `canSee`+射程交火」切換推進權重——不能交火就推進、能交火就拉開距離對射，避免在射程外卡點或自殺貼臉。**調敵人侵略性從這兩個下手。**
3. **bloom 手動專屬、待機另計**：早期把 bloom 套到待機導致待機噴 30° 在 12 秒被秒；現分流（手動 bloom＝技巧、待機固定基礎+bloom 但 TAP 靠停頓壓住）。
4. **恆朝上 + FPS mouselook**：見 §4.2 取捨。
5. **迷霧離屏遮罩**：見 §4.3 踩雷。
6. **全隨機招募、無基準**：使用者接受失去固定基準，換取每輪 run 不同；個性＝抽人的風險層。
7. **WANDER 收斂到 idleAnchor**：原本整圈亂抽像慌張平民；改為只在進入閒置正前方 ±28° 小幅擺動。
8. **撤離面向移動方向 + 自動接手**：撤離不再用待機掃視朝向（看起來像在逃）；且「不再附身任何人」時自動接手（非撤離優先），不留空轉。

---

## 9. 本 session 開發脈絡（時間線，依序）

1. 從規格建出 Phase 1 垂直切片（主迴圈/地圖/附身/待機FSM/敵AI hysteresis+reservation/撤離/軟計時/結算）。
2. 戰術化：移速放慢、發散 bloom、換彈/彈藥、射速加快。
3. 附身旋轉視角（恆朝上）→ 後改為 FPS 滑鼠鎖定（解決「游標不置中就永遠轉」）。
4. 戰爭迷霧（敵我、90°錐+近身）；修「迷霧看不到敵人」改離屏遮罩。
5. 敵人堅守朝向 waypoint。
6. 撤離後鏡頭續跟。
7. 底部單位卡片 + 武器資料模型(含重量/迴轉/稀有度) + 技能(heal 佔位)。
8. 休息關 + 場景狀態機（戰鬥→休息→戰鬥）。
9. 路線圖（MAP/COMBAT/REST/RUNEND/RUNWIN）；戰鬥改成「滑鼠鎖定後才開始動」。
10. 修 MAP 場景崩潰（`update` 在判場景前碰未建立的 `cam`）。
11. Shift 奔跑（倍率隨武器 weight）。
12. 待機個性矩陣（ARCHETYPES）+ 全隨機招募 + 紀律接 bloom+彈藥。
13. WANDER 收斂到 idleAnchor + 附身恢復正面到 idleAnchor。
14. 鏡頭 anchorY（被附身單位偏下、前方視野更長）。
15. 撤離面向移動方向 + 鏡頭同向（`idleTick` lockFacing）。
16. 「不再附身單位即自動接手（非撤離優先）」保險。
17. 跨關回血旋鈕 `interMissionHealFrac`（預設 1.0＝自動全補；攜帶當前 hp，調低即帶傷壓力）。
18. **庫存層**：共用倉庫(有上限/綁生還人數) + 臨時背包 + 戰鬥掉落/拾取 + 搜刮改三選一(地板保證) + 休息關配裝/換技能 + 共用彈藥儲備與手動分配 + 底線武器 pistol（§4.9/4.10）。
19. **地形/互動/敵FSM**：兩張戰鬥地圖(MAPS registry + `buildMapDerived`) + 門/低矮牆/按鈕 + E 互動 + 敵人 IDLE↔ENGAGED(latch)；bravo＝門後藏閒置敵（§4.11/4.12）。
20. **路線圖可見化 + 保證**：bravo 標到 node 1/6/9(終點必經)，路線圖加顏色/圖示/標籤/圖例。
21. **關卡編輯器**（scene EDITOR）：視覺放置/驗證/試玩/輸出 JS（§4.13）。
22. 微調：門互動改「到門線段最近點」量距 + interactRadius 40；低矮牆 slow 0.5→0.3、touchR 6→12。
23. **Phase 3**：裝備+道具各 1 槽 + 軍械庫節點 + 任務內俘虜 + 各關時間；編輯器加俘虜工具/時間欄（§4.14/4.15）。
24. **敵人類型系統**：`ENEMY_TYPES`(grunt/dog/heavy_lmg/heavy_shot) + 武器 `pierce`(rifle0.5/dmr0.9) + 裝甲「機率整發擋下非穿甲」(`armor.blockChance`0.85) + 攻擊模組(ranged/shotgun pellets/lmg burst) + 警犬廣視錐(逐單位 coneHalfDeg)；放進 alpha/bravo + 編輯器 spawn 循環擴充（§4.16）。**自此 git 化，每次完成 push 到 GitHub repo**。
25. **撤離 QOL**：撤離鍵 F→**H**（防誤觸，hysteresis 移到 J）；**Shift+H 全體撤離**；**撤離護送模型**＝附身/切到撤離中單位不再中斷，持續自走+滑鼠轉視野，任一指令(移動/開火/技能/換彈/道具/互動)才奪回控制；退役 `followExtractId`（§4.7）。
26. **側翼/突襲系統**：`damageUnit` 加 `fromAng`；繞背(錐外)＝×2 且無視所有裝甲(敵擋格+玩家dmgResist)、閒置敵正面＝×1.5、取高不疊、**敵我皆適用**；`spawnCrit` 紅火花；`CONFIG.flank`（§4.17）。
27. **第0關 + cutscene 導演系統**：`intro` 地圖 + 通用節拍導演（say/move/face/wait/cam/sub/do/shoot/gate）；序章演出＋教學（處決教開火、E 取碼/開門、守衛入隊、H/Shift+H 撤離）→序章休息(教學面板)→路線圖；陣容=主角+2守衛；Esc 只跳演出；`initRun` 改先進序章（§4.18）。
28. **微調**：鎖定前黑螢幕(全關卡 `drawCombatStart`)；兩守衛依序密語；第0關聚光迷霧 + 對話泡(取代底部框，舊版備用)。
29. **I/O 接縫重構**（不改行為）：抽出 `Input`(模擬讀輸入唯一介面)/`Sfx`(音效 hook) + PORT MAP 註解，為將來搬 Phaser 預留「便宜那半」（§14）。
30. **潛行層 Stage 1（警覺核心）**：`canSee` 依狀態(idle 短錐+有限視距)、識別空窗(`detect` 累積，快閃安全/貼臉瞬識/槍聲擊中仍瞬交戰)、overwatch 對 IDLE 收火、識別回饋弧；`CONFIG.stealth`（§4.19）。
31. **潛行層 Stage 2-5（一口氣）**：草叢(MOBA 遮蔽+drawEnemies 依 isRevealed+編輯器 bush 工具)、消音手槍 silpistol+亞音速彈(不發槍聲/稀少)、望遠(右鍵：鏡頭前帶+穩定+禁奔跑，技能移空白鍵，留 toggle/sens 鉤子)、前哨站 outpost(分層 choke 哨點+草叢，roadmap node5)（§4.19）。
32. **地圖重設計**：舊圖「整牆+單缺口」逼擠同洞、側翼/潛行無從施展 → alpha→**開闊庭院**(掩體島+草叢+路網)、outpost→**前哨站v2**(中央短牆+上下繞道)、新增 **lanes 雙線哨廊**(平行低矮牆+換道)；bravo 保留為破門原型。舊片段備份於 `舊地圖備份.md`；無頭幾何測試確保邊不穿牆（§5）。
33. **可破壞地形**：`MAP.destructibles`＋形態輪廓 `DESTRUCT`(wall/glass/lowwall/fence/flat)，被擋下的子彈扣 HP→換 broken 形態；木箱/落地窗/有矮牆窗/全遮蔽/鐵網五型；玻璃非穿甲擋穿甲穿、敵我皆可破；接 segBlocked/collideWalls/onLowWall/updateBullets；編輯器 destruct 工具+預設；alpha 放四型示範（§4.20）。爆裂桶/可射熄燈延後。
34. **小修**：第0關同事演出期永遠可見(drawEnemies 對 lock 豁免 isRevealed)；陣亡留地上叉叉(corpses)。
35. **數值平衡面板**：`` ` ``/齒輪開的 DOM 側欄，依物件結構自動生成滑桿/勾選，直接寫 CONFIG/WEAPONS/… 活值＝即時生效；四分頁+搜尋、localStorage 持久化、輸出覆寫/全部重設；資料層與 DOM 分離(headless 47 條驗證含面板建構+全場景逐幀+戰鬥中改值)（§4.21）。**概念驗證告一段落，轉入真正關卡/平衡製作；新獨立地圖編輯器待規劃核可（§11）。**

> 註：上表跨多個 session；§1「現況總覽」是最新狀態的權威來源，時間線僅供脈絡。

---

## 10. 已知簡化 / 暫緩

- `targeting` 只實作 `NEAREST_IN_LOS`（MOST_EXPOSED/FIRST_SEEN 以最近代替）。
- 技能/裝備/道具皆佔位數值；**各 1 槽（武器/技能/裝備/道具），用 id 欄位實作、未來可改陣列開多槽**。裝備效果＝stat 倍率；道具第一版只做瞬發+短時 buff（**投擲物/煙幕/區域效果延後**）。
- 撤離只有快路（無偏 cover 的穩路）。
- 彈藥共用儲備 `stockStart600`、分配寬裕（機制與 UI 已做齊，但本階段不咬人）。
- 路線圖節點 4 型：COMBAT/REST/ARMORY/(終點)；俘虜＝任務內目標物（撤離成功才入隊）。各關時間 node>map>全域可設（編輯器有欄位）。
- 搜刮「質升級」：personality＝隨機升一 tier、discipline＝強制 TAP（per-member 覆蓋）；皆佔位、可再細緻。
- 難度遞增只是「每關 +1 rusher」暫定。
- 兩張戰鬥地圖(alpha/bravo)、單一休息；路線圖只有 2 型節點。
- **敵人狀態機**：IDLE＝守點(固定 watchFacing、不巡邏不掃視)；ENGAGED 後才用「全域最近我方」選點推進（IDLE 不再有全域我方知識，較潛行）。ENGAGED latch 永不回退（刻意）。
- **低矮牆「敵人繞行」靠 waypoint 邊不穿越低矮牆**（無真尋路）；若把低矮牆擺在敵人邊上，敵人會頂著牆卡住——地圖設計約束，已用無頭測試把關。
- **門 vs 自走單位 softlock**：自走(撤離/閒置)單位不會開門；關門卡撤離路會停住，需玩家回頭附身開門。bravo 已避開，但新圖要注意。
- `drawRest` 仍讀 `lastSummary.mission`：只因 `ROADMAP.start` 皆 COMBAT 才安全（REST 不會是第一個）；若把 REST 放首位需加防護。
- 場外低血我方目前只有卡片紅字 + 邊緣點，無強烈場外低血警示。

---

## 11. Backlog（含已與 chat 討論的方向）

- **早期休息關**：路線圖早點安插休息節點，當作抽到爛個性時的調裝補救出口（✅ 休息關「裝備調整」本體已做＝§4.10；剩「早點安插」的路線設計）。
- **+1 技能槽 / 多技能戰鬥**：本階段刻意延後（單槽）。資料層要做需 `abilitySlots`+技能陣列、戰鬥多施放鍵（數字鍵已被換人佔用，需另綁 Q/E 等）。
- **彈藥真咬人**：本階段 `stockStart/defaultAlloc` 寬裕。要讓彈藥成為壓力＝調低這些值（機制已齊）。
- **地形延伸**：更多門/低矮牆地圖、門開關動畫(`openT` 漸變)、敵人巡邏(IDLE 掃視/走動)、低矮牆真繞行(局部避讓)、自走單位卡門的場外提示。
- Phase 2 其餘：第三種節點（經濟的戰鬥/俘虜護送）、buff 兩軸(AUTOPILOT/HANDS)、任務內資源/俘虜、source/sink 經濟。
- Phase 3：更多武器/技能效果(非 heal)、穩路撤離、AI/可見度調校、平衡、場外低血警示。
- **敵人類型延伸**（§4.16 已落地 grunt/dog/heavy_lmg/heavy_shot + 裝甲/穿甲）：Marksman/狙擊型、護盾兵、自爆兵；穿甲做成裝備(`fx:{pierceAdd}`)或消耗品讓玩家主動取得；玩家可撿/用敵專屬武器(目前 fangs/lmg/shotgun 不掉落)；裝甲分級(部位/方向)、近戰真擊(目前借射擊管線)。
- 可立即接的小項：奔跑代價(耗體力/降精度)、非 heal 技能效果、MOST_EXPOSED 實作、難度曲線、搜刮質升級細緻化、更多 bravo 風格地圖。

---

## 12. 驗證方式與踩雷

- **無頭測試法**（每次改完都跑）：抽出 `<script>`，在 Node `vm` 沙箱 stub 掉 `document/window/performance/requestAnimationFrame` 與 Proxy canvas ctx（離屏 canvas 用 `document.createElement` stub、需 `document.exitPointerLock` stub、fakeCanvas 需 `style:{}`），移除結尾 `requestAnimationFrame(frame)` 後逐幀驅動。
- **務必模擬真實迴圈**：在所有場景（含 MAP）逐幀跑 `update()+draw()`，否則會漏掉「非戰鬥場景碰 cam/players」這類崩潰。
- 踩雷清單：①`update` 必須先判 `scene!=='COMBAT'` 再碰 `cam/players`（MAP 時 cam 未建立）。②迷霧不可在主畫布 destination-out（要離屏）。③TTK 旋鈕請見「§8 設計取捨」與下方。
- 典型基準：放置小隊與第一波對拼後殘存者撐到追兵湧入才被折（可信但不足）；hysteresis ON 明顯降低敵人選點抖動。

### TTK 旋鈕速查（之前討論過）
- 單體爆發 TTK：目標 HP、`weapon.damage × fireRate`。
- 有效 TTK：發散(spread/bloom)、range、projSpeed。
- 持續 TTK：magazine/reloadTime（備彈 9999 故目前只有換彈空檔咬人）。
- 交火 TTK：反應延遲、迷霧/朝向、手動 vs 待機(idle 三倍率)、集火數。
- 注意搜刮會讓我方 maxHp 一路 +15 累積，run 後段我方 TTK 變長。

---

## 13. 與原規格 `SLEEPER_開發計畫.md` 對應

- §1 設計憲章：全數遵守（待機弱、個性固定（現為招募時隨機抽、之後固定該角色）、切換無CD、軟計時非失敗、down=dead、≥1撤出過關、不寫空間尋路）。
- §4 待機策略 → 擴成 §4.5 個性矩陣（facing 多了 WANDER、新增 discipline 軸）。
- §5 角色/武器/技能資料模型 → 已資料驅動（WEAPONS/ABILITIES/ARCHETYPES）；加上共用倉庫/掉落/配裝/共用彈藥儲備（§4.9/4.10）。
- §6 敵人 AI → 完成（hysteresis/reservation/charge-hold/watchPoint）+ IDLE↔ENGAGED 純狀態機(看到/聽到/被打 latch)（§4.12）。
- §7/地形 → 門/低矮牆/按鈕 + 互動(E) + 第二張圖 bravo（§4.11）。
- §8 撤離與軟計時 → 完成（快路）。
- §9 roguelite 結構 → 路線圖 + 休息關(搜刮三選一/喚醒) + 庫存/裝備/彈藥 meta 完成；經濟/俘虜未做。
- §13 里程碑：Phase 1 完成；Phase 2 進行中。

> 記憶檔（`.claude/.../memory/`）另有 `sleeper-phase1.md`、`planned-idle-personality.md` 與索引 `MEMORY.md`，內容與本文件一致，可交叉參考。

---

## 14. 架構分層 / 搬遷預留（I/O 接縫）

為將來「也許搬到 Phaser」預留，已做**便宜那半**重構（不改行為、無頭驗證過）。三層：
1. **純模擬**：`update(dt)` 及其呼叫鏈（AI/彈道/附身/庫存/roadmap/cutscene…），不碰 `ctx/DOM`，與引擎無關。
2. **接縫**（`index.html` 輸入區，`const Input` / `const Sfx`，附 PORT MAP 註解）：
   - **`Input`**：模擬讀輸入的**唯一介面**——`Input.held(k)`/`Input.fire`/`Input.locked`/`Input.aim`(讀寫)/`Input.mx,my`。目前是 getter 代理背後狀態（`keys`/`mouse`/`pointerLocked`/`aimDir`），那些由 DOM 事件處理寫入＝**目前的輸入轉接器**。`update`/`extractTick`/`possess`/`toggleExtract`/`extractAll`/`startMission`/`startIntro` 已全改走 `Input.*`（模擬層不再直接命名背後狀態）。
   - **`Sfx.play(id)`**：音效 hook（目前 no-op，標出「何時該出聲」）。已接：`shot/shotgun`(fireUnit)、`reload`(startReload)、`hit/block/crit/death/heal/pickup`(各 spawn*)、`door`(tryInteract)。將來接 WebAudio/Phaser 只改這一處實作。
3. **轉接器**：DOM/canvas 事件處理（click/mousedown/mousemove/keydown/pointerlockchange）＝目前實作；Phaser 版改寫這層（寫同一份背後狀態，或重定義 `Input`/`Sfx`）即可，**模擬層與無頭測試不動**。
- **昂貴接縫刻意延後**（等真的決定搬遷再做）：render 門面（`ctx` 用量 ~853）、迷霧離屏遮罩、旋轉相機、編輯器接線。判別法：grep `ctx`/`cv`/`document` 落在邏輯函式裡的都是待收的接縫。
- 紅利：模擬層純化後，無頭測試（測 `update`+狀態）**搬遷後照用**。
