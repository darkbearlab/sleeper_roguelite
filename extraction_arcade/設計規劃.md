# 搜打撤變體 — 設計規劃（MVP 已實作 · 2026-06-26）

> **狀態：MVP 已動工並可玩**（使用者 2026-06-26「邊做地圖邊把搜打撤第一版做一做」）。實作＝`extraction_arcade/index.html`（當時 index.html 的完整複本＋疊上 arcade 層；**與主程式 index.html 不再自動同步**）。boot 直接進 `enterArcade`。headless 驗證 28/28。
> 原始指示（2026-06-23）：另開此專屬目錄維護本變體、由 SLEEPER **直接複製分家**（不抽共用引擎、不與 index 維持耦合，行為各自演化）、陣營先**玩家本位 + 獵人 FFA**。
>
> ## MVP 實作摘要（已完成）
> - **進入流程**：boot → **開始前設定畫面 `ARCADESETUP`**（`enterArcadeSetup`／`drawArcadeSetup`）＝選 地圖（大型搜索場 / 開闊庭院小圖）、戰爭迷霧（正常 / **全開 debug**）、獵人數、寶物數 → 「開始」`enterArcade`。選項 `arcadeOpts`（localStorage 持久）＋`arcOpt(key)` 覆寫 CONFIG。`CONFIG.arcade`（treasureCount / pickupRadius / hunters / hunterSatisfy / costPerShot / map='bigyard' / **backpackSlots / hunterSlots / lootTypes / salvageByRarity**）。
> - **大型地圖（參數化、自由大小、每局不同）**：`makeBigArcadeMap(opts)` 程序生成格狀 waypoint 路網（保證連通、掩體放格子中心故不壓 waypoint 邊＝無 AI 卡死）。**地圖寬/高（格）可在設定畫面自由輸入**（clamp 5~22 兼顧效能）＋預設快捷（中/大/特大/巨大/超巨）；`applyArcadeMapSize()` 依 `cols/rows` 重生成 `MAPS.bigyard`。**掩體與住民配置吃 srand＝每局布局不同**（每格 ~22% 實體/26% 草叢/14% cover/16% lowcover/22% 空）。`drawGrid` 隨 `MAP.width/height`。獵人數/寶物數也自由輸入。alpha 仍可選為小圖。
> - **設定畫面自由數值輸入**：`numField` 點值框→鍵盤打字→Enter/點別處 commit（clamp 套用）＋[−]/[+] 步進。
> - **獵人性格（積不積極）＋任意人數 FFA**：每名 holder/flanker/rusher（輪替），且**每名各自一勢力（`'hunter'+i` 動態註冊 FACTIONS）＝不論幾名都互敵不結盟**。`hunterEngageRange`＝rusher 無限見人就打／flanker 中距／holder 幾乎只自衛；被擊中暫時放大會回擊。**搜滿即帶寶逃（無視交火）**＝玩家看得到有人成功撤離；謹慎型看到遠方敵人也不理、繼續搜寶。整體再放慢識別（`CONFIG.arcade.identifyTime/idleViewRange`）＝住民不秒鎖玩家。修了「沒人達成滿意值、開場全在對轟」。
> - **迷霧全開（debug）**：設定 noFog → `arcadeNoFog`＝跳過 `drawFog` 且 `drawEnemies`/`drawScreenOverlays` 不再 cull 未感知敵＝整張圖+所有單位可見（不畫 debug 節點圖）。
> - **寶物（分類）**：`treasures[]`＝每局吃種子在 waypoints（或 `MAP.treasureSpots`）抽子集；**每顆抽一個分類**（`lootTypes`：現金/情報/醫療品/軍火/藝術品，各有 `slots` 佔格、`values` 價值池、`weight`、`hue`）。迷霧之下要搜才看得到（`drawTreasures` 畫在 fog 之前、依類上色依格放大）。
> - **★格數背包**：`u.bag`（玩家/獵人共用）＋`bagSlots/bagValue/bagCap/bagFits/bagAdd`。撿寶與撿地上掉落武器（`updateDrops` arcade 分支＝`salvageByRarity` 折算價值＋`slotsOf` 佔格）都**裝得下才入袋**；滿了提示按 **G `dropLastLoot`** 丟最後一件（生成地上寶物可再撿，也會被獵人搶）。獵人陣亡 `killUnit`→`scatterBag` 散落地上。
> - **獵人**：各自成一勢力（`FACTIONS.hunterA/B…`＝FFA）、`e.hunter`＋隨機 `satisfaction`＋`_bagCap`。`enemyTick` IDLE/失去目標→`hunterIdleTick`：`lootSeekStep`（認領→`bfsPath`→撿、`nearestUnclaimedTreasure` 略過裝不下的）→ `bagValue≥satisfaction` 或背包滿 → `hunterExtractStep`。
> - **環境威脅**：地圖 enemySpawns（住民 grunt 巡邏）＋2 隻野怪（dog、會遊蕩），faction `enemy`、敵對所有人。
> - **★計分/排行榜**：`fireUnit` 玩家開火累 `arcadeCost`；撤離→`arcadeResults`＝loot＝`bagValue`、`score = 撤出寶值 − 成本`、**陣亡＝所得歸零**；entry `{score,loot,items,date}` 排序取 `leaderboardMax`、算本局名次 `rank`/`newRecord`；`arcadeScores`（localStorage `sleeper_arcade_scores_v1`）。scene `ARCADERESULT`（名次/寶值/件數/日期、本局高亮、★新紀錄、再來一局/清除排行榜/回主選單）＋`drawArcadeHUD`（背包格數條/寶值/淨值/flash）＋`drawCombatStart` arcade 開場顯 top-5。
> - **重用**：COMBAT 引擎/附身/敵 AI/`extractTick`/迷霧/juice/多陣營 `hostile`。`?menu` 進母遊戲主選單（沙盒/編輯器）。headless 驗證 24/24（大圖連通、0 邊穿牆、設定套用、迷霧全開、各場景 draw）。
>
> ## 待續（下一步候選）
> 陣營敵對矩陣細緻化（現全 FFA）、撤離點伏擊、時間壓力（reinforce 計時，arcade 目前關閉）、寶物的編輯器工具 + 地圖 `treasureSpots`、附身小隊深度、線上排行榜、更多大圖/地圖變化。

## 一句話定位
2D 俯視「**搜打撤**」(search → raid → extract) arcade。與 SLEEPER 同一套引擎，但**複製成獨立分支**維護——「很多行為和魅力應該從此分家」。

## 架構決定（使用者拍板）
- **複製分家**，不共用 `engine.js`、不破壞 SLEEPER 的單檔自足：動工時把當下的 `index.html` 複製到本目錄當起點，之後完全獨立改。
- 可重用基底（從 SLEEPER 帶來、再各自演化）：戰鬥/附身、敵 AI（IDLE↔ENGAGED FSM、巡邏 `patrolStep`、搜索/獵殺 `huntStep`＋共享情報、`bfsPath` 圖上尋路、近戰收尾衝刺）、撤離 `extractTick`、戰爭迷霧、打擊感 juice、地圖 registry＋編輯器（含 `editor.html`）、沙盒、I/O 接縫（Input/Sfx）、數值面板。

## 世界與勢力
- **玩家**：找寶、隨時可撤。
- **野怪**（＝改寫的警犬 `dog`）：巡邏。
- **住民**（＝巡邏的 `grunt`）：巡邏。
- **獵人**（各式人類敵人）：走訪 waypoint 網路、找到**有寶物標記的 waypoint**、拾取累值。
- **陣營（v1 決定）**：以**玩家本位**為基底（野怪/住民敵對玩家、以巡邏為主）；**獵人先做成 FFA**——彼此攻擊、見到其他勢力(玩家/野怪/住民/別的獵人)就打。完整敵對矩陣的細緻化暫緩，先 FFA。
  - 實作要點：把現有二元的「找最近敵人」目標選取（`nearestFriendly`/`nearestKnownPlayer`）一般化成「**最近的敵對他勢力單位**」；共享情報改為**逐勢力**。

## 核心機制
- **寶物**：waypoint 上的寶物標記 + 價值；玩家與獵人皆可拾取。傾向**每局隨機生成**（設計師標可生成點 → 每局抽子集 + 亂數價值）＝耐玩。
- **獵人尋寶 AI**：去最近**未被認領**的寶物點（沿用 `bfsPath` + 認領制）→ 拾取累積價值 → **`累積價值 ≥ 該獵人的滿意值` → 轉往撤離點離場**。每個獵人**滿意值不同**（有人小賺就走、有人貪心）。
- **玩家結算**：**無固定額度目標**；離場時統計「**消耗成本 vs 取得價值**」，盈餘＝得分。建議 `score = 撤出價值 − 耗用成本(彈藥/道具/…)`；**陣亡＝損失本局所得**（搜打撤精髓：死了什麼都沒了）。細節動工前定。
- **本機排行榜**：`localStorage`（線上排行榜之後再說，先 localhost）。
- （可選）**時間壓力**：待越久、獵人/危險越多（沿用 reinforce 計時）→ 強化「見好就收」的拉扯。

## 相對 SLEEPER 的新系統
1. **陣營/FFA 目標選取**（二元 → 多勢力；先 FFA）。
2. **寶物模型 + 拾取**（玩家 + AI）+ 編輯器「寶物」工具。
3. **獵人「尋寶 → 滿足 → 撤退」目標棧**（建在現有尋路/認領/撤離上）。
4. **成本/價值結算 + 本機排行榜**（新 meta + localStorage）。
5. **換皮**：野怪/住民/獵人（多為現有 dog/grunt + 新增人類敵人變體）。

## MVP（動工時的最小可玩核心）
一張圖：寶物每局隨機生成 → 玩家撿寶 + 撤離 → `score = 價值 − 彈藥成本` → 本機排行榜；野怪 + 住民巡邏並獵殺玩家；1–2 名獵人會尋寶 + 滿足撤退、且 FFA。跑通後再加陣營深度、撤離點伏擊、時間壓力。

## 暫緩 / 待議
線上排行榜；完整敵對矩陣（先 FFA）；成本與得分的精確公式；寶物生成的設計師可控程度。
