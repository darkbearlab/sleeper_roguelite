# SLEEPER (codename)

俯視戰術射擊 × 附身式小隊操作 × Slay-the-Spire roguelite。**單一檔 `index.html`**（原生 JS + Canvas 2D、零外部相依，瀏覽器開檔即玩）。

## 變體（規劃中、尚未動工）
- `extraction_arcade/` — 2D「搜打撤」arcade 變體，將由本遊戲**複製分家**獨立維護（非共用引擎）。**目前只有設計文件 `extraction_arcade/設計規劃.md`，尚未動工**；非經使用者明確要求別開工。本目錄與 SLEEPER 主程式無關，改 index.html 時可忽略。

## 接手前先讀
- **`開發進度與脈絡.md`** — 現況架構、系統、CONFIG 速查、踩雷（2026-06-24 重整精簡版）。**恢復實質工作前先讀這份，別重新摸索程式碼。**
- `封存/` — 舊版完整逐功能紀錄/時間線/舊 §4.x 編號（需考古再翻；現況以上面那份為準）。
- `SLEEPER_開發計畫.md` — 原始設計規格。

## 速記
- 所有可調數值/內容在 `index.html` 檔頂：`CONFIG` / `WEAPONS` / `ABILITIES` / `EQUIPMENT` / `ITEMS` / `ARCHETYPES`(3 型待機個性) / `PASSIVES`+`PASSIVE_TREES`(被動技能樹) / `ENEMY_TYPES`(敵人類型) / `DESTRUCT` / `MATERIALS`(區域地形材質) / `MAPS` / `FALLBACK_ROADMAP`(保底圖)。
- **隨機路線圖生成（`genRoadmap`，吃種子）**：每局(initRun／startFinale 在 `srandSeed` 後呼叫)程序生成路線圖＝StS 式 `pathCount` 條路徑聯集(第 0..cols-2 欄)＋最後欄補**單一王**(人人必達)。類型：首欄 COMBAT、**王前一欄全 REST**(整補)、中段 `CONFIG.roadmap.typeWeights` 加權、王＝COMBAT(BOSS 圖)。戰鬥圖依**地圖 `roles` 標籤**分池抽(`combatMapPool('normal'|'boss')`；`eventMap`/未標 roles 的圖不入池)＋避免連續同 `category`。`validateRoadmap`(連通/唯一終點)失敗→退回 `FALLBACK_ROADMAP`。`let roadmap`(作用中)＋`buildRoadAdj()`(重建 `roadAdj`/`_maxCol`)；`roadNode`/`drawRoadmap`/`selectNode`/`advanceNode` 皆讀 `roadmap`。**長度 `CONFIG.roadmap.cols`(固定可調，非隨機)**。**地圖標籤**：內建戰鬥圖加 `category`(類型)+`roles`(`['normal']`/`['boss']`/兩者)；alpha=open/normal、lanes=corridor/normal、bravo=facility/both、outpost=outpost/both、finale=facility/boss。editor 標籤 UI 待做。
- **數值平衡面板（§4.21）**：按 `` ` `` 或右上齒輪開的 DOM 側欄，依物件結構自動生成滑桿/勾選、直接寫上列目錄的活值＝即時生效（武器是共用參考即時；彈夾/unitHp/敵人hp,armor 下次生成套用）。資料層(`tune*`/`applyTunerSaved`/`tuneOverrides`/localStorage 鍵 `sleeper_tuning_v1`)與 UI 層(`initTunerUI`，全包 try/catch、無 DOM 即停用)分離。新增 CONFIG 數值不必改面板。
- **獨立地圖編輯器 `editor.html`（§4.22，與 index 分檔）**：自足 DOM 工具，三分頁(地圖物件/waypoint+連線/敵人配置)、拖曳、連線編輯加強、邊穿牆紅標、與 MAPS 同結構雙向 JSON round-trip＋「MAPS 片段」貼回。**v2**：★特徵庫(`FEATURES` 柱子/房間/長廊/轉角/窗牆/掩體叢/十字路口，拖框 `stampFeature` 放置成扁平物件)、**框選群組** `selRefs`(marquee+8 控制點整組縮放/位移，**不存持久 `_gid`**、格式仍與遊戲一致)、重疊放置+點擊循環 `hitObjectsCycle`、**門切牆缺口** `cutWallsForDoor`、**參考底圖** (第4分頁「底圖」，`underlay` 墊圖描繪可旋轉，editor-only 不匯出，存 `sleeper_editor_underlay`)、**地圖尺寸可調** (`map.width/height` 真欄位+標題列輸入+「外牆」重建；遊戲相機不夾 WORLD 故本就支援大地圖)、**新增 waypoint 自動連線** (`autoLink` 開關+`autoLinkWaypoint` 連所有中間無阻隔點)。**踩雷：刪 waypoint 必須重編號**(遊戲 `wp=id=>MAP.waypoints[id]` 以陣列索引當 id，連帶 edges/enemySpawns.nodeId)。editor.html 自帶一份小常數(ENEMY_TYPE_IDS/ENEMY_META/WP_TAGS/DESTRUCT_PRESETS/FEATURES/TEMPLATES)，改 index 對應內容時要手動同步。in-index 編輯器(§4.13)仍並存。**工具列分群**(地圖庫/本圖/檢視/單張＋「？」`EDITOR_HELP` 說明)；**地圖左下角 1 公尺比例尺**(`PX_PER_M=40`/`drawScaleRef`)；**鏡像 ⇄/⇅**(`mirrorMap`/`mirrorToNew`：剛性翻轉成庫裡新的一張、保留原圖＝做地圖變體；敵朝向 H`π−θ`/V`−θ`)。**v3 多地圖庫(maps.json)**：editor 變成多地圖管理器＝以 `maps.json`(`{鍵:地圖}`)為單位「讀入整份→下拉切換/＋新增/🗑刪除/改鍵→下載整份」。model 層 `mapLib/curKey`＋`libImportObj/libExportObj/selectMap/addMap/deleteMap/renameCurKey/syncCurrentToLib`(DOM 分離、headless 可測)；通用清理抽成 `cleanOf`。**index.html 完全不動**——maps.json 只裝自訂地圖、不含內建圖，遊戲端怎麼吃 maps.json 之後再做。
- 地形：牆/門/低矮牆/草叢(MAP.bushes)/**可破壞地形(MAP.destructibles：wall/glass/lowwall/fence/flat 兩形態+HP)**；五型預設(木箱/落地窗/有矮牆窗/全遮蔽/鐵網)。改地形阻擋邏輯看 segBlocked/collideWalls/onLowWall/updateBullets/canSee。詳見 §4.20。
- **區域地形(MAP.areas=[{x,y,w,h,mat}])＋牆體厚度**：地形抽象成 `MATERIALS` 材質表(move/los/bullet 三軸)＋**自由矩形**殼。五材質：solid(實體/柱/建築)・cover(大型掩體/車輛,擋視機率擋彈)・barrier(壕溝/欄杆,走不過但看穿打穿)・lowcover(矮掩體叢,越看越射+減速)・smoke(煙幕,走得過打得過但看不穿,靜態)。線性家族(牆/門/窗)維持**線段+固定厚度**(`CONFIG.wallThick`=8px≈20cm；子彈/視線以「離中心線≤半厚」撞渲染牆面＝`segSegDist`)；移動仍走單位半徑 capsule、迷霧視錐維持中心線。區域用 `segHitsRect`/`rayRectT`/`pushOutRect` 接到同 5 接縫＋fog。**可摧毀區域暫不做**(只靜態)。**踩雷同低矮牆：擋移動的區域不可壓 waypoint 邊**(編輯器 edgeBlocked 紅標)。editor.html 有「區域」工具(拖框+材質選)，mirror/cleanOf/normalize 都吃 areas(自帶 MATERIALS 副本要手動同步)。
- 敵人＝資料驅動類型（`enemySpawns.type`，預設 grunt）：**grunt(步兵·衝鋒/rusher)・grunt_flank(步兵·包抄/flanker)**・dog(警犬)・heavy_lmg・heavy_shot；重裝靠裝甲「機率整發擋下非穿甲」(武器 `pierce`)而非高血量。詳見 §4.16。
- **多陣營（搜打撤前置；N 個易擴充）**：`FACTIONS` 登錄表(`{id:{name,hue}}`，目前 player/enemy/rioter)＋通用 `hostile(a,b)=a!==b`(不同陣營互敵、同陣營友軍)。**要加新勢力＝加一條 FACTIONS + spawn 用其 id，不必改碼**(搜打撤的多個抱團小組各成一勢力＝FFA+組內結盟自然成立)。唯一特殊 id＝`'player'`(可操控、在 `players`)，其餘皆 AI(在 `enemies`，faction 任意)。改動的接縫：`makeEnemy(…,faction)`＋`enemySpawns[].faction`、子彈命中`[players+enemies].filter(hostile(b.faction,t.faction))`、`damageUnit` 的 enemy 專屬邏輯改 `faction!=='player'`(任一AI)、`registerHitThreat` 只分享同陣營、`enemyTick` 索敵加 `nearestHostileAI(e)`(看到他陣營AI＝立即ENGAGE、與 `nearestKnownPlayer` 取較近者；對玩家仍走潛行/知識系統)、`drawEnemies` 用 `factionHue(e.faction, 類型色)`(enemy hue=null→沿用類型色維持原樣)。2 陣營時行為完全等同舊版(縮退)。`MAPS.riot`「暴動街區」(`eventMap`)＝駐軍(enemy)vs暴民(rioter)開幕互毆、玩家趁亂繞行；沙盒下拉可選 riot 測試。**已接成戰役事件**：`EVENTS` 的 `id:'riot'`(combat-hook `{type:'combat',map:'riot'}`，另含安全繞路選項)＝EVENT 節點隨機會出現「趁亂穿過」。搜打撤 fork 用此多陣營土台。
- **敵人近戰收尾**（§4.6）：`enemyTick` 移動＝若 `seen && weapon.range ≤ CONFIG.enemy.meleeApproach(60)` 則直奔「目標本人」(f.pos) 而非節點，補上節點導航到不了咬擊距離的最後一段（只有 fangs=34 觸發，長射程武器永不）。性格(rusher/flanker/holder)目前只驅動**選點走位**，不改開火/距離管理（待重設計，§4.6 末）。
- **IDLE 巡邏**（§4.6）：`type.patrols` 的單位(只有 grunt)IDLE 會巡邏；重裝/狗守原地。`enemyIdleTick`→`patrolStep`：認領制 `patrolClaims`(隨機認領未佔點→`bfsPath` 走→`startDwell` 停留掃視→放掉再認領，點>兵=輪換不可預測)；巡邏池 `computePatrolPool`＝有 patrol 標籤點(≥2)用之否則自動取距入口/撤離口都遠的點。轉 ENGAGED/死亡 `releasePatrol`。`CONFIG.patrol{dwell,speedMul,autoBandFromEnds}`。
- **開關門（§4.6）**：`openDoorsNear(e,track)` 走到關門就開（**狗 opensDoors:false 不開**）；IDLE 巡邏開路上門＋`closeDoorsBehind`(走遠且門口無人才隨手關回)；**交戰後永不關門、只開**(進 ENGAGED 清 openedDoors)；玩家自走撤離 extractTick 也開門→門＝減速帶非死牆、**softlock 解除**(免額外 waypoint)。
- **被擊中來向局部脅威（§4.6，修側翼魚貫被側射全滅）**：`registerHitThreat`＝被擊中(連被甲擋下)時，在「被擊中者→射手」方位(`fromAng`)距 `hunt.threatDist` 放脅威點，傳給**本人＋看得到他的敵＋`hunt.threatShareRadius` 內近隣**(皆 ENGAGED)，存 per-enemy `threatPos/threatT`(只給方向、非全體 lastSeen＝保潛行)。enemyTick：無全體已知目標但有新鮮 threatPos(≤`hunt.threatGrace`)→當合成目標 `threatOnly`＝朝來向回頭/推進但**不對空地開火**；親眼看到真玩家才正常開火。持續開火持續刷新方位、停火移動可甩。`CONFIG.hunt{threatGrace,threatShareRadius,threatDist}`。
- **交戰後共享情報+搜索（§4.6，修視野穿牆）**：`updateThreatKnowledge()`(每幀，enemyTick 前)算我方「被任一敵看到否」＝共享。**KNOWN**(有人看到→全體共享實位追擊、更新 `player.lastSeenPos`；ENGAGED 用 `nearestKnownPlayer`，選點吃已知位置不穿牆、開火吃實位且需本隻 canSee)→**SEARCH**(全員失視線→`searchFocusNode`=最後已知位置最近節點、`searchSet`=bfsWithin hops、走到/視線掃過清格)→**PATROL**(清光沒找到→全場 patrolStep 含重裝、待新目擊再建焦點)。`CONFIG.hunt{searchHops3,knownGrace0.5}`。
- 側翼/突襲（§4.17，敵我皆適用）：繞背(目標視野錐外)＝傷害×2 且無視所有裝甲；閒置敵正面＝×1.5；`damageUnit` 第4參 `fromAng`。
- **手榴彈道具（§4.24b，不用新系統）**：`throwGrenade` 朝瞄準方向**丟出一顆會飛的手榴彈**(`grenades` 陣列，`rayHit` 算落點不穿牆、`throwRange/throwSpeed`)→`updateGrenades` 飛抵落點**停住等引信(`ITEMS.grenade.grenade.fuse` 秒，落地不立即爆)**才 `detonateGrenade` 360° 散射一批 `bullets`(faction 玩家)。威力衰減＝被幾顆打中(中心密/遠處稀)、超 range 過期。pierce0 對重裝弱。`drawGrenades` 畫飛行中的小罐＋越近爆越快的警示燈。
- **煙霧彈道具（沿用手榴彈飛行管線）**：`ITEMS.smoke`(`smoke:{throwRange,throwSpeed,radius,dur}`)→`throwSmoke` 推一顆帶 `smoke` 標記的飛行物→`updateGrenades` 飛抵落點**即 `spawnSmoke`**(無引信無散射)＝在 `smokes` 陣列生一團煙幕(`{x,y,r,t,max}`，`updateSmokes` 倒數 `dur`(預設5s)消散)。**煙幕＝暫時草叢遮蔽**：`inSmoke(pos)`＝在半徑內**且與落點 `clearLine` 無牆阻隔**(＝有效區域不穿牆)；`concealAt`=`inBush||inSmoke` 統一遮蔽區，`inConceal`/`canSee` 的 MOBA 隱蔽判定改吃 `concealAt`(目標在遮蔽區、觀察者不在同區→看不到；近身 senseRadius 仍穿透)。`drawSmoke` 灰色柔光雲(生成擴張/消散淡出)畫在 `drawWalls` 之前＝牆會蓋住＝視覺也不穿牆。
- **閃光彈道具（沿用手榴彈飛行+引信管線）**：`ITEMS.flash`→`throwFlash` 推帶 `flash` 標記飛行物→`updateGrenades` 引信(`fuse`)燒完 `detonateFlash`＝360° 噴一批**不傷害**彈丸(`bullets` 帶 `flash:perHit`、`dmg:0`)。`updateBullets` 命中分支：`b.flash`→`t.flash=min(max, t.flash+perHit)`(累積暈眩、不傷害＝**中越多顆越暈**，中心密)。暈眩飽和度 `flashF(u)=min(1,u.flash/max)` 縮放效果：**`canSee` 視距變短(rangeMax→rangeMin)＋視錐變窄(coneShrink)**、`enemyFire` 發散×(1+f·spreadAdd)、敵 `moveToward` ×`flashMoveMul`(減速)；`u.flash` 每秒 `decay` 衰減(敵 update 迴圈)。faction-tagged＝玩家丟只暈敵人。`flashFx` 爆閃環(`drawFlashFx` lighter)＋白粒子＋敵頭頂 `✦` 脈動圈(drawEnemies)。值在 `CONFIG.flash`/`ITEMS.flash`。
- **消耗品堆疊（`ITEM_STACK_MAX=3`）**：道具槽改成充能制——單位帶 `itemId`+`itemCount`(0~3，取代舊 `itemUsed`)。**取得一次＝整疊3顆**(所有 item 入庫走 `giveItemStack(id)`＝`{kind:'item',id,count:3}`；event/搜刮/軍械庫/撿取皆是)。倉庫 item 條目帶 `count`。休息配裝 `equipGearFromWarehouse` 對 item 特例＝**同道具堆疊到上限3、不同道具整疊換出、超過上限退回倉庫**；`unequipGear` 退整疊。`useItem` 每次 `itemCount--`(0＝用完不可用)。`rosterFromPlayer` 帶回剩餘 `itemCount`(0 則丟棄)。HUD/配裝列/倉庫格皆顯 `×N`。沙盒選道具給滿一疊。
- **視覺化聲音（§4.25，無音效替代）**：`Sfx.play(id,pos)` 給 pos→`pingSound`(`SOUND_PINGS` 表)→`soundPings`；`drawSoundEdges` 對控制角色 `!canSee` 的 ping 在畫面外框該方向畫發光（你眼前/自己的聲音不提示，背後/牆後才亮）。`CONFIG.soundEdge`。引擎級、arcade 沿用。
- **開火現形（§4.24，修草叢隱形射手）**：敵人非消音開火 → `e.revealT=CONFIG.stealth.fireReveal`(0.6s)；`isRevealed`＝`revealT>0 || 任一我方 canSee`，`drawFog` 對 revealT>0 敵人挖洞→草叢/霧中正在開火的敵人對玩家現形（槍口火光暴露，與友軍 overwatch 一致）。
- 潛行/警覺層（§4.19，Stage 1-5 全做完）：IDLE 敵短錐+有限視距(`canSee` 依狀態)+識別空窗(`enemy.detect` 累滿才 ENGAGED，永鎖；槍聲/被擊中瞬交戰)+overwatch 對 IDLE 收火；草叢(`MAP.bushes`/`inConceal`，MOBA 遮蔽，`drawEnemies` 依 `isRevealed`)；消音手槍(`WEAPONS.silpistol` silent，亞音速彈稀少)；望遠(右鍵 `isScoping()`，鏡頭前帶+穩定+禁奔跑，技能移空白鍵)；前哨站 `MAPS.outpost`。`CONFIG.stealth`/`scope`。
- **每單位交戰姿態（§4.19）**：我方 `player.stance`（makePlayer 預設 `'defend'`）：`defend`＝overwatch 只打已 ENGAGED 敵(原行為)／`engage`＝打視野內任何敵(主動開戰)。`idleTick` 篩選＝`u.stance!=='engage' && e.state!=='ENGAGED' → skip`；`setStance(u,s)`。**鍵位變更：E＝engage、Q＝defend（對 controlledId 設、不奪回護送）；互動 E→F、道具 Q→C**。engage 單位頭頂紅「⚔」。
- **雙武器 + 小刀**：每單位**兩個武器槽**(主+副，副預設 pistol 後備)。**X 或滾輪換槍**(`swapWeapon` 對調 active↔`u.weapon2`；戰鬥中 `cv` 的 `wheel` 監聽器→`swapWeapon(controlled)`，`swapT` 冷卻自動防連刷)：`u.swapT` 冷卻＝`CONFIG.weaponSwap.base + 抽出武器 weight×perWeight`(越重抽越慢)、換槍期間不能開火、重算 active turnSpeed。每槽自帶彈藥/換彈/bloom 狀態。負重＝`carriedWeight`(主+副總重)吃進 `sprintMult`(帶兩把跑較慢＝代價)。idle/敵人只用 active(主)。**配裝卡主/副武器分開兩列顯示**(`weaponBlock(yt,slot)`：名稱列＋彈藥列各槽各自 equip/▸倉/彈藥±；不再用 `restWslot` 小字切換；卡片加高 ch=176)；equip/卸下/彈藥吃 `slot` 參數(`setMemberWeapon/equipWeaponFromWarehouse/unequipWeapon/allocAmmo(…,slot)`)；沙盒有副武器選單。roster 帶 `weaponId2/allocatedAmmo2`。新武器 **knife 小刀**：攻速同手槍、range42 貼臉、weight0.1、dmg45、pierce0.9、`melee`(彈藥無限不換彈)＝解甲副武器。`isInfiniteAmmo`＝底線 pistol 或 `melee`。
- **機密檔案（任務目標物，§4.26）**：run 級唯一物件，獨立攜帶格、休息關配裝卡單選指派攜帶者（`roster.carriesFile`/`player.hasFile`，`ensureFileCarrier` 恆一名、跨關隨撤出者）。攜帶者陣亡→`fileDrop` 掉地（穿迷霧永遠可見＋HUD 警示＋邊框箭頭），任一活動我方走過即背（含自走撤離，`updateFile`）。**失敗條件**：戰鬥結算無「撤出成功者帶著檔案」→ RUN FAILED（`loseReason`，與全滅同級）。第0關/沙盒/編輯試玩不啟用。改名：發射密碼→機密檔案。
- **密封容器（事後抽選拾取物，搜打撤前置）**：`MAP.caches=[{x,y}]` 配置。`cacheGround[]`(地上)＋`u.caches`(運搬個數)。比照機密檔案：`updateCaches` 走過即背、`killUnit` 運搬者陣亡掉地(`cacheGround`)、`drawCacheDrops` 穿迷霧青色 `?` 標記＋HUD 攜帶數＋邊框箭頭、任一活動我方再撿。**但非必須**(掉/未取＝無報酬，不致 RUN FAILED)。**撤離才開封**：`endMission` 過關時每個 `caches` 跑 `rollMysteryLoot()`→進倉庫(run 限定)＋戰果結算 `drawSettle` 顯「密封容器開啟：…」。`rollMysteryLoot`＝抽類型→抽內容，**排除基本階(common 稀有度／技能 heal,firstaid)、排除 noRandom(Siren)、不過 availPool(含未解鎖)**。**Siren 永不進任何武器抽選**(`WEAPONS.siren.noRandom`＋`randomWeaponPool()` 過濾，rollLootWeapon/randomMember/mystery 皆套；手動選仍可)。關卡 `MAPS.vault`「金庫」(`eventMap`)＝守備房A/B 各一開關→雙層門(airlock `do`/`di`)→金庫內密封容器；事件 `id:'vault'`(combat-hook)。沙盒下拉可選。**前瞻**：`slotsOf(kind,id)`(現全1、大槍2)＝未來 slot 背包(攜行格數制·stash+背包兩層)的資料先備；現倉庫條目 `{kind,id,count}` 加 `slots` 即可遷移、cache 模型直接對應。
- **開發測試架（`CONFIG.devLootRack`，預設 true、發佈前設 false）**：第0關地上陳列所有武器/道具(一般掉落)＝撿進背包→序章休息配裝；`enterRest` 背包→倉庫已支援 item/equip。`spawnDevRack`。
- 第0關/cutscene 導演（§4.18）：`initRun` 每局先 `startIntro()`（`intro` 地圖、scene COMBAT + `cutscene` 節拍導演，可 Esc 跳）→ 序章休息 → 路線圖。通用導演 say/move/face/wait/cam/sub/do/shoot/gate 可重用於未來劇情。改 update/draw/initRun 時注意 `cutscene`/`introActive` 分支。
- 場景：`MENU / HQ / FINALE / ENDING / MAP / COMBAT / REST / SETTLE / EVENT / ARMORY / RUNEND / RUNWIN / EDITOR / SANDBOX`（有遊戲內關卡編輯器）。
- **休息＝純整補**(`rollResupply`：抽類型→抽內容、彈藥可複數、全進倉庫/儲備、零風險、無抉擇)；**風險/取捨/歪 build 集中在事件格**。
- **事件節點 `EVENT`(scene EVENT)**：路線圖 `?` 紫節點 → `enterEvent` 從 `EVENTS` 資料目錄隨機抽一個(本局避重)→ 選項套 `applyEventOutcome`(outcome 只用現有資源:ammo/loot/heal/damage(有下限不致命)/maxhp/weapon/equip/item/recruit/**relic 跨樹被動**/trade(付裝備)/gamble(知情機率)/**combat**) → `eventAdvance`→路線圖。準則：**每事件都有安全選項、風險知情、下檔有上限**。`need:'target'` 走 TARGET 子階指定成員。`drawEvent/eventClick/eBtn`。**★combat hook**：`{type:'combat',map}`→`forcedMap`+`startMission`(接特製短地圖；打完走一般結算→advanceNode)。**事件內容待一個一個敲**(目前 4 個種子;`EVENTS`/`EVENT_CFG` 面板/檔頂可調)。
- **戰役旗標 + 種子 RNG + 連動事件（地基已做）**：`runFlags`(跨節點記玩家選擇,`setFlag/flag`,initRun 重置)；事件 `cond:f=>…` 出現條件、`scripted:true`(只能被佇列觸發不隨機)、選項 `set:{}`/`queue:[]`(寫旗標/排後續)；`pendingEvents` 佇列＝enterEvent 優先播(節點仍顯 `?`＝「被報位」的伏擊)。outcome 加 `rest`(事件→休息關)。**單一 run 的非戰鬥 RNG 全吃一顆種子**(`srand`/`srandSeed`,initRun 播種；`rngPick`/`weightedPick`+rollResupply/gamble 已改吃 srand＝可重現;戰鬥仍 `Math.random`；`runSeed` 角落淡色顯示 `drawSeedTag`(frame() 疊繪))。**cutscene 對話「選項」節拍已做**：beat `{kind:'choice',text?,pos?,options:[{label,fn?,set?,queue?,then?:[beats]}]}`＝列選項、**按數字鍵 1~9 選**(演出鎖滑鼠故用鍵盤)、選後套 fn/set/queue + 把 `then` 節拍接到後面續播(分支)。`beatEnter/beatDone` 的 `choice` 分支 + drawCutscene 渲染 + keydown/mousedown 攔截。**scene-kind 事件已做**：`EVENTS` 條目 `kind:'scene'`＝借序章管線在戰鬥地圖上演(`startSceneEvent`：`onEnter` 擲隱藏身分→`forcedMap`+`startMission` 生我方+演員→`cutscene` 跑對白/選項)；收場由腳本 `do` 拍呼叫 `eventResolveAdvance`(和平→路線圖)/`eventResolveRest`(變休息)/`eventResolveCombat`(解鎖開打→敵 ENGAGED→一般結算)。`eventActive` 旗標。演員＝**`civ` 敵人型**(拿 pistol、`cutscene.lock` 期間不行動，之後換皮)。專用小地圖 **`meet`/`ambush`**(`eventMap:true`＝不召難度遞增兵/追兵)。**男孩事件**(`boy` scene + `boyAmbush` scripted)：隱藏身分(友善/敵意吃種子) × 相信/拒絕 4 分支(友善信→休息、友善拒→離開、敵信→當場開打、敵拒→離開+`pendingEvents` 排 `boyAmbush`＝之後 `?` 變伏擊)。`civ` 要同步 editor.html 的 ENEMY 常數(目前未同步、編輯器放不了 civ，無妨)。
- **成長弧：個性技能樹 + 戰果結算升階**：我方個性＝**3 型 archetype**(磐石 Anchor·HOLD/固守、先鋒 Spearhead·SWEEP/推進、遊獵 Ranger·TRACK/戰場生存;只看 facing 軸,discipline/fireMode 為每型固定預設)。每型一棵**被動樹**(`PASSIVE_TREES`,3 階×二選一=18 條,各階獨立)。**升階在戰果結算(scene SETTLE)做**：COMBAT 勝利→`enterSettle`→挑一名撤出者二選一升一階(`settlePromote`,tier 0→3、學該階配對之一)→`settleAdvance` 回路線圖。**休息＝轉職**(`cycleArchetype` 換樹、保留已學被動,後續升階抽新樹)＝weird build 來源。被動效果＝**條件式戰技**(訓練非超能),在戰鬥 hook 動態套(`passive*` 函式+`CONFIG.passive` 面板可調)：fireUnit(發散/傷害/射速/bloom/槍聲/子彈標記)、updateBullets 命中(破門手依距離/壓制/擊殺 onKillPassive)、damageUnit(免暴擊/減傷)、idleTick(警戒待命)、移動(passiveMoveMul)、makePlayer(靜態類:破門手轉向/快手換彈/斥候視錐感知)、tickPassives(每幀靜止/衝鋒窗/buff/脫戰回血)。第0關/沙盒/編輯不到結算。
- **局外進度（meta 解鎖，鉤子已做、內容待加）**：跨 run 持久 `meta`(localStorage `sleeper_meta_v1`：`intel` 情報點 + `unlocked{weapon/equip/archetype/ability}` + `stats`)。**橫向解鎖為主、不破壞局內平衡**：基礎內容全可用;未來新內容掛 `UNLOCKS`(目前空)＝`{kind,id,via:'milestone'(cond)|'intel'(cost),name}`。`isAvailable/availPool` 把招募/loot 的 weapon/archetype/equip/ability 池**過濾未解鎖**(空時 no-op)＝解鎖只是「進池」、遊戲中仍 RNG 取得。`metaOnRunEnd(won)`(在 RUNWIN/RUNEND 呼叫)＝給情報點＋累計 stats＋`checkMilestoneUnlocks` 自動解里程碑。**情報點公式(加分為主、扣分只是扣意思的)**：`gross = 關數×intelPerMission + 勝×intelWin(通關保底) + 事件數×intelPerEvent`；陣亡扣分 `min(陣亡×intelPerKIA, gross×kiaPenaltyCapFrac)` 封頂(預設最多扣 25%)；`gain = max(0, gross−扣分)`。本局累計 `runStats{events,kia}`(initRun 重置；events 在 `enterEvent`++、kia 在 `killUnit` 我方死++且非沙盒/試玩/序章)。明細存 `lastIntelBreakdown`，RUNWIN/RUNEND 畫面 `drawIntelBreakdown` 顯示。`unlockWithIntel(kind,id)` 花情報點解鎖。**分工(使用者定)**：里程碑解 archetype/技能、情報點解 武器/裝備。`loadMeta` 在 boot。`saveMeta` 變更即存。**HQ 解鎖介面已做(§4.28)**＝主選單「總部」進。**種子 UNLOCKS(4 項)**：`WEAPONS.ar`(突擊步槍,情報90)、`WEAPONS.revolver`(左輪,情報60)、`ABILITIES.surge`(戰地手術大補,里程碑「通關1次」自動解)、`WEAPONS.siren`(無限彈步槍/全場槍聲,via:`finale`＝通過最終挑戰解、解後每場自動入倉庫)；前三掛進 RECRUIT/LOOT/ABILITY 池(未解鎖自動濾掉)、siren 不進 RNG 池(固定起始裝備)。`drawHQ` 對 `via:'finale'` 顯「通過最終真相挑戰解鎖」。**待做**：更多 UNLOCKS 內容慢慢加。
- **主選單（進入點，§4.27）**：開機預設 `goMenu()`＝canvas 畫的 `MENU` 場景四選項（戰役→`initRun`／自由戰鬥→`enterSandbox`／**總部→`enterHQ`**／地圖編輯器→`location.href='editor.html'`，`drawMenu`/`menuRects`/`menuClick`）。**重設計＝下方「完成度／戰況」strip**(正面回饋)：情報點/通關/清關/解鎖 X/Y ＋內容量(武器/幹員/被動/技能/裝備/道具/事件/敵型/地圖數，由各目錄 `Object.keys().length` 即時算)＋已完成系統勾選列。`?sandbox` 仍直接沙盒；`?playtest` 走 `bootPlaytest`。結算(RUNEND/RUNWIN)點擊/R＝回選單。
- **最終真相挑戰（§4.29）**：通關 `CONFIG.finale.revealWins`(預設3)次後 → `goMenu` 攔截先播一次「真相揭示」純文字(`FINALE_REVEAL`，存 `meta.revealSeen`)→ 主選單多紅色「最終潛入」入口(`drawMenu` items `hot`)。`enterFinaleSetup`(scene FINALE，畫布打字輸種子緩衝 `finaleSeedBuf`，keydown 接收/Backspace/Enter)→`startFinale`＝**正式打一整輪 run（非單關）**：srandSeed(種子 hash `seedFromStr` 或隨機)、`baseRoster()` 組隊、重置路線圖(`roadAvailable=ROADMAP.start.slice()`)、`finaleActive=true`、**跳過序章直接 `scene='MAP'`**(用一般路線圖 maps/節點，鐵人＝中途仍有 settle/event/rest)。**檔案機制關閉**(startMission `!finaleActive`)→`endMission` 過關條件改 `survivors≥1 && (finaleActive || fileSafe)`；**收尾不在每關**：`advanceNode` 走完整條路線(終點)→`resolveFinale(true)`、`endMission` 全滅→`resolveFinale(false)`。`resolveFinale(won)`：勝→`grantFinaleUnlock`(解 `meta.unlocked.weapon.siren`)+真結局 `FINALE_TRUE_END`；敗→Bad End `FINALE_BAD_END`。(MAPS.finale 已不用、留作備用地圖)。**最終解鎖＝`WEAPONS.siren`**(致敬 The Patriot：`infinite:true`＝彈夾不耗永不換彈、`loud:true`＝開火 `alertEnemiesNear` 全場半徑＝無法潛行；`isInfiniteAmmo` 認 infinite、fireUnit `if(!W.infinite) ammoInMag--`)。解鎖後 `grantStartingGear()`(initRun 呼叫)每場自動把 siren 放進 warehouse；重挑戰 finale 時領隊直接配 siren。**結局＝純文字 scene ENDING**(`playEnding(lines,onDone,title)`/`endingTick`(frame() 用真實 delta 計時逐行出)/`endingAdvance`(點擊/空白：未出完→加速全出、已出完→onDone)/`drawEnding` 黑底白字漸顯)。兩種結局 onDone 都 `goMenu`＝**回到挑戰前繼續玩，無刪檔/無軟鎖**。draw/click/keydown 皆接 FINALE+ENDING 分支。
- **總部 HQ（§4.28）**：主選單「總部」→`enterHQ`(先 `checkMilestoneUnlocks`+`saveMeta` 補播里程碑)→`drawHQ` 列 `UNLOCKS`：已解鎖✓／里程碑(顯條件 `u.req`、達成後自動解)／情報點(顯 cost＋「解鎖」鈕，情報足才可點＝`unlockWithIntel`)。頂部顯情報點/出擊/通關/清關。`hqRects`/`hqClick`/`hqMsg`，返回鈕＝`goMenu`，Esc/Backspace 回選單。draw/click/keydown 已接 `HQ` 分支(`update` 非 COMBAT 早退故安全)。
- **編輯器測試戰鬥（跨頁往返，§4.27）**：editor.html「▶ 測試戰鬥」→`playtestCurrent`(目前地圖存 `localStorage.sleeper_playtest`＋整庫存 `sleeper_editor_session`)→`index.html?playtest=1`→index `bootPlaytest` 讀圖、`enterSandbox` 預填地圖 JSON、`playtestMode=true`→打完/離開(`endMission`/`exitSandbox`/Backspace) `location.href='editor.html'`。回到 editor 由 `restoreSession` 還原整個工作區（不丟稿）。
- **自訂戰鬥 SANDBOX（§4.23）**：左上「⚔ 自訂戰鬥」鈕或 `?sandbox=1`。無路線圖/演出，自選小隊(個性/主武器/副武器/技能/裝備/**道具**＋🎲隨機)＋貼地圖JSON或內建下拉→打一場(敵人沿用地圖 enemySpawns)。**所有下拉皆 `availPool(kind,…)` 過濾＝未解鎖內容不顯示**(siren/ar/revolver/surge 等局外解鎖前不出現)；`makeSandboxMember` 也 `isAvailable` 二次把關(存檔帶鎖定內容也不套用)。`addMemberRow` 加 `item` 槽(吃 `availPool('item',ITEM_IDS)`)，cfg/header/randomMember/defaultMember 皆已串 `itemId`。重用 startMission/endMission，靠 `fromSandbox` 路由(打完/Backspace 回設定)。`initSandboxUI`(try/catch)；設定存 `sleeper_sandbox_v1`。
- **子彈/曳光彈渲染（§4.24，`drawBullets`）**：一般彈＝**極細淡線**僅提示彈道(`CONFIG.juice.bulletAlpha`，0＝完全隱形；不再畫亮頭 blob)；**曳光彈＝火光色**(橙黃、敵偏紅)較長亮拖尾(lighter)。**只有 `WEAPONS[id].tracer:true` 的槍有曳光彈**(目前 lmg/siren；大部分槍無)，且**每 `CONFIG.juice.tracerEvery`(5) 發才一發**(現實 NATO 4:1≈第5發；`fireUnit` 用 `u.shotCount % every` 標 `bullet.tracer`)。**武器射程改真實＝大部分跨全場**(pistol/revolver→1500；silpistol→900、shotgun→700＝亞音速/霰彈的近距例外；近戰除外；無距離傷害衰減，迷霧本就限制可視/可瞄)。
- **打擊感 juice（§4.24，`CONFIG.juice`）**：近 hitscan 子彈(`bulletSpeedMul`，碰撞本就掃描式不穿透)、頓幀(`hitstop`/`hitstopT`，主迴圈凍 update，僅擊殺)、螢幕震動(`shakeAmt`/`kickY`/`addShakeAt` 依距相機衰減，套在 `applyCam`+`w2s` 的 `shakeX/shakeY`)、開火後座(`u.recoil`)+槍口閃焰；**Batch 2**：受擊閃白(`u.hitFlash`)+後仰(`u.flinchX/Y`)+血霧(`spawnBlood`)+地上血漬(`decals`/`pushDecal`/`drawDecals` 上限160)。**我方死亡鏡頭已取消**(使用者：激戰中相機轉過去干擾操作)＝陣亡只保留短頓幀 `hitstop(deathHitstop)`，無相機帶位/慢動作/暈影。音效(WebAudio)暫不做。
- 完成的更新都 commit + push 到 GitHub `darkbearlab/sleeper_roguelite`（git 認證走系統 Git Credential Manager）。
- 改完一定要驗證：抽出 `<script>` 丟 Node `vm`、stub 掉 DOM/canvas/pointerlock/**localStorage**，**逐幀跑遍所有場景的 `update()+draw()`**（不要只在戰鬥跑）。
- 必踩雷：① `update()` 必須先判 `scene!=='COMBAT'` 再碰 `cam/players`（非戰鬥場景未建立）。② 迷霧用離屏 canvas 遮罩，**不可在主畫布 `destination-out`**（會擦掉世界）。③ `MAP` 已非單一 const 而是 registry 的作用中指標：衍生表(`N/adj/nodeVis/nextHop/EXTRACT/ENTRY`)由 `buildMapDerived(key)` 重建，別假設它們是載入期常數。
- I/O 接縫（§14，為將來可能搬 Phaser 預留）：模擬層讀輸入只走 `Input.*`(held/fire/locked/aim)、出聲只走 `Sfx.play(id)`；DOM 事件處理＝轉接器。**新增戰鬥邏輯時別直接讀 `keys`/`mouse`/`pointerLocked`/`aimDir`，改用 `Input`**。render 門面/迷霧/相機等昂貴接縫尚未抽（仍直接用 `ctx`）。
- 細節別寫進本檔（每 session 自動載入，保持精簡）；重內容放 `開發進度與脈絡.md`。
